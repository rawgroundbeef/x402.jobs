---
plan_id: 28-01
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/Projects/x402jobs-api/src/routes/upload.ts
  - ~/Projects/x402jobs-api/src/routes/escrow.ts
  - ~/Projects/x402jobs-api/src/routes/runs.ts
  - ~/Projects/x402jobs-api/src/routes/wallet.ts
  - ~/Projects/x402jobs-api/src/routes/__tests__/upload.test.ts
  - ~/Projects/x402jobs-api/src/routes/__tests__/escrow.test.ts
  - ~/Projects/x402jobs-api/src/routes/__tests__/runs.test.ts
  - ~/Projects/x402jobs-api/src/routes/__tests__/wallet.test.ts
  - .planning/phases/28-security-review/REVIEW.md
autonomous: true
requirements: [HIGH-04, HIGH-06, HIGH-12]
estimated_effort: Small
risk: Low
must_haves:
  truths:
    - "req.body.userId is never read in routes/upload.ts; all path/ownership derivations use req.user!.id"
    - "Escrow webhook secret comparison uses crypto.timingSafeEqual with length pre-check; webhook_secret is mandatory (no optional path)"
    - "Public response shapes in routes/runs.ts and routes/wallet.ts redact payer_address (first 6 + last 4) and hash payment_signature (SHA-256 first 16 chars)"
    - "Internal/admin shapes still carry full payer_address and payment_signature"
    - "Baseline test count is preserved or higher; new tests for HIGH-04, HIGH-06, HIGH-12 pass"
    - "REVIEW.md HIGH-04, HIGH-06, HIGH-12 sections marked '**Status:** FIXED'"
  artifacts:
    - path: "~/Projects/x402jobs-api/src/routes/upload.ts"
      provides: "Upload endpoints that bind file ownership to req.user!.id only"
    - path: "~/Projects/x402jobs-api/src/routes/escrow.ts"
      provides: "Escrow webhook with timing-safe secret check"
    - path: "~/Projects/x402jobs-api/src/routes/runs.ts"
      provides: "Public run shapes redact payer/signature"
    - path: "~/Projects/x402jobs-api/src/routes/wallet.ts"
      provides: "Public wallet activity shapes redact payer/signature"
  key_links:
    - from: "routes/upload.ts"
      to: "req.user.id (authMiddleware)"
      via: "auth middleware injects req.user; handlers must not pull userId from body"
      pattern: "req\\.user!?\\.id"
    - from: "routes/escrow.ts"
      to: "crypto.timingSafeEqual"
      via: "Buffer.from(provided), Buffer.from(expected) after length match"
      pattern: "crypto\\.timingSafeEqual\\("
    - from: "routes/runs.ts and routes/wallet.ts"
      to: "redactPayer / hashSignature helpers"
      via: "Applied in public response mappers; internal/admin paths untouched"
      pattern: "redactPayer|hashSignature"
---

<objective>
Bundle three independent "trivial wins" from REVIEW.md into one PR-sized plan: HIGH-04 (upload userId injection), HIGH-06 (timing-safe escrow webhook), HIGH-12 (payer/signature redaction in public APIs).

Purpose: These are the lowest-risk, highest-merge-velocity HIGH findings. Shipping them first builds momentum and reduces the surface area before BSL 1.1 public release.

Output: Three remediated routes, four test files updated, REVIEW.md status flipped to FIXED for each finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-security-review/28-CONTEXT.md
@.planning/phases/28-security-review/REVIEW.md
@.planning/phases/28-security-review/HIGHS-TRIAGE.md
</context>

<interfaces>
Target codebase: `~/Projects/x402jobs-api/` (translate REVIEW.md paths `apps/api-audit-tmp/` → `~/Projects/x402jobs-api/`).

Key existing primitives to reuse:
- `req.user` is injected by `src/middleware/auth.ts` (authMiddleware). All three upload handlers in scope already use `authMiddleware`; `req.user!.id` is available.
- `crypto.timingSafeEqual` is a Node built-in. Buffers must be equal length; pre-check `Buffer.byteLength(a) === Buffer.byteLength(b)` before calling.
- `crypto.createHash('sha256').update(sig).digest('hex').slice(0, 16)` for HIGH-12 signature hashing.
- Truncation helper for HIGH-12: `addr.slice(0, 6) + '...' + addr.slice(-4)`.
</interfaces>

<tasks>

<task type="verification">
  <name>Task 1: Pre-flight check</name>
  <read_first>
    - ~/Projects/x402jobs-api/.git/HEAD (confirm on main)
    - ~/Projects/x402jobs-api/src/lib/safe-fetch.ts (must exist — confirms CRIT-07 merged)
    - ~/Projects/x402jobs-api/src/routes/api-keys.ts (must reference key_hash — confirms CRIT-03 merged)
    - .planning/phases/28-security-review/HIGHS-TRIAGE.md ("Pre-flight checks" section)
  </read_first>
  <action>
    1. `cd ~/Projects/x402jobs-api && git status` — must be clean.
    2. `git fetch origin && git checkout main && git pull origin main`.
    3. Confirm CRIT fixes present: `test -f src/lib/safe-fetch.ts` and `grep -q "key_hash" src/middleware/apiKey.ts`.
    4. Record baseline: `pnpm vitest run --exclude '**/resource-registration*' 2>&1 | tail -5 | tee /tmp/28-01-baseline.txt`. Extract test count.
    5. Confirm no stale CRIT branches: `git branch -a | grep -v 'main\|HEAD\|fix/crit-07'` should be empty (CRIT-07 branch may linger; OK).
    6. Create working branch: `git checkout -b fix/high-batch-a-trivial-wins`.
  </action>
  <verify>
    <automated>cd ~/Projects/x402jobs-api && git status --porcelain | grep -q . && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `cd ~/Projects/x402jobs-api && git rev-parse --abbrev-ref HEAD` outputs `fix/high-batch-a-trivial-wins`
    - `cd ~/Projects/x402jobs-api && test -f src/lib/safe-fetch.ts && echo OK` outputs OK
    - `cd ~/Projects/x402jobs-api && grep -q "key_hash" src/middleware/apiKey.ts && echo OK` outputs OK
    - `/tmp/28-01-baseline.txt` exists and contains a Tests line with a count ≥ 260
  </acceptance_criteria>
  <done>Branch checked out, baseline test count recorded, no stale CRIT branches.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: HIGH-04 — remove userId-from-body in routes/upload.ts</name>
  <files>~/Projects/x402jobs-api/src/routes/upload.ts</files>
  <read_first>
    - ~/Projects/x402jobs-api/src/routes/upload.ts (all of it; the three handlers at lines 74-110, 158-220, 282-340)
    - ~/Projects/x402jobs-api/src/middleware/auth.ts (confirm req.user!.id shape)
    - .planning/phases/28-security-review/REVIEW.md (HIGH-04 section, line 128)
    - .planning/phases/28-security-review/28-CONTEXT.md (Batch A spec for HIGH-04)
  </read_first>
  <behavior>
    - Test 1: POST /upload/* with `userId: 'attacker-uuid'` in body while authenticated as `victim-uuid` results in file path scoped to `victim-uuid`, NOT `attacker-uuid`.
    - Test 2: POST /upload/* with NO userId in body still works and uses `req.user!.id`.
    - Test 3: Any handler-level destructuring of `userId` from `req.body` is gone.
  </behavior>
  <action>
    In `src/routes/upload.ts`:
    1. Find every occurrence of destructuring `userId` from `req.body`. Per REVIEW.md these are at lines 74-110, 158-220, 282-340 — three handlers. Pattern to remove (typical shape):
       ```ts
       const { userId, ...rest } = req.body;
       // ... later ...
       generateFilePath(uploadType, fileName, fileType, userId || req.user?.id)
       ```
    2. Replace with:
       ```ts
       const { ...rest } = req.body; // userId intentionally NOT read from body (HIGH-04)
       const authUserId = req.user!.id;
       // ... later ...
       generateFilePath(uploadType, fileName, fileType, authUserId)
       ```
       Where the current code uses `userId || req.user?.id`, replace the entire expression with `req.user!.id`. Where any DB row write previously used the body `userId`, swap to `req.user!.id`.
    3. Do NOT silently drop a `userId` body field validation — if the request schema previously accepted it, either:
       - delete it from the zod/joi schema if present, OR
       - keep accepting it for backward compatibility but ignore it (preferred — log a one-line `console.warn('[upload] ignored userId from body; using authenticated user')` and proceed).
    4. Leave file-content checks (mime/size/etc.) untouched.
  </action>
  <verify>
    <automated>cd ~/Projects/x402jobs-api && ! grep -nE "const \\{[^}]*userId[^}]*\\}\\s*=\\s*req\\.body" src/routes/upload.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd ~/Projects/x402jobs-api && grep -cE "const \\{[^}]*userId[^}]*\\}\\s*=\\s*req\\.body" src/routes/upload.ts` outputs `0`
    - `cd ~/Projects/x402jobs-api && grep -cE "userId\\s*\\|\\|\\s*req\\.user" src/routes/upload.ts` outputs `0`
    - `cd ~/Projects/x402jobs-api && grep -cE "req\\.user!?\\.id" src/routes/upload.ts` outputs ≥ 3 (one per handler)
    - `cd ~/Projects/x402jobs-api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three upload handlers derive userId exclusively from req.user!.id; no body destructuring of userId remains; typecheck passes.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 3: HIGH-06 — timing-safe escrow webhook secret comparison</name>
  <files>~/Projects/x402jobs-api/src/routes/escrow.ts</files>
  <read_first>
    - ~/Projects/x402jobs-api/src/routes/escrow.ts (specifically lines 72-80 and 95-100 — the two comparison sites)
    - .planning/phases/28-security-review/REVIEW.md (HIGH-06 section, line 137)
    - .planning/phases/28-security-review/28-CONTEXT.md (Batch A spec for HIGH-06)
  </read_first>
  <behavior>
    - Test 1: Wrong-length provided secret returns 401 immediately (no timing-safe call attempted).
    - Test 2: Wrong-value same-length provided secret returns 401.
    - Test 3: Correct secret returns 200/expected success path.
    - Test 4: Missing `expectedSecret` (env var unset) → endpoint returns 500/refuses to operate, NOT silently passes.
  </behavior>
  <action>
    In `src/routes/escrow.ts`:
    1. At the top of the file, add (if not already present): `import crypto from 'crypto';`
    2. Find the two comparison sites at lines 72-80 and 95-100. Current pattern:
       ```ts
       if (providedSecret !== expectedSecret) {
         return res.status(401).json({ error: 'Unauthorized' });
       }
       ```
       or any variant using `===`/`!==` on the secrets.
    3. Replace each with:
       ```ts
       if (!expectedSecret || typeof expectedSecret !== 'string') {
         console.error('[escrow] ESCROW_WEBHOOK_SECRET not configured; refusing webhook');
         return res.status(500).json({ error: 'Server misconfigured' });
       }
       const providedBuf = Buffer.from(providedSecret ?? '', 'utf8');
       const expectedBuf = Buffer.from(expectedSecret, 'utf8');
       if (
         providedBuf.length !== expectedBuf.length ||
         !crypto.timingSafeEqual(providedBuf, expectedBuf)
       ) {
         return res.status(401).json({ error: 'Unauthorized' });
       }
       ```
    4. If the current code has an "optional secret" pattern like `if (expectedSecret && providedSecret !== expectedSecret)`, REMOVE the optional gating — secret is now mandatory. The first guard above handles the unset case.
    5. Apply the same replacement at both line ranges (72-80 and 95-100).
  </action>
  <verify>
    <automated>cd ~/Projects/x402jobs-api && grep -c "crypto.timingSafeEqual(" src/routes/escrow.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd ~/Projects/x402jobs-api && grep -c "crypto.timingSafeEqual(" src/routes/escrow.ts` outputs ≥ 2 (one per comparison site)
    - `cd ~/Projects/x402jobs-api && grep -cE "providedSecret\\s*!==\\s*expectedSecret|providedSecret\\s*===\\s*expectedSecret" src/routes/escrow.ts` outputs `0`
    - `cd ~/Projects/x402jobs-api && grep -q "ESCROW_WEBHOOK_SECRET not configured" src/routes/escrow.ts` exits 0
    - `cd ~/Projects/x402jobs-api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both escrow webhook secret comparisons use crypto.timingSafeEqual with length pre-check; optional-secret path removed; typecheck passes.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 4: HIGH-12 — redact payer_address and hash payment_signature in public APIs</name>
  <files>
    ~/Projects/x402jobs-api/src/routes/runs.ts
    ~/Projects/x402jobs-api/src/routes/wallet.ts
    ~/Projects/x402jobs-api/src/lib/redact.ts
  </files>
  <read_first>
    - ~/Projects/x402jobs-api/src/routes/runs.ts (specifically lines 312-405 — public run shape)
    - ~/Projects/x402jobs-api/src/routes/wallet.ts (specifically lines 289-371 — public wallet activity shape)
    - .planning/phases/28-security-review/REVIEW.md (HIGH-12 section, line 167)
    - .planning/phases/28-security-review/28-CONTEXT.md (Batch A spec for HIGH-12)
    - ~/Projects/x402jobs.git/../x402jobs-api/src/types (or wherever the run/wallet types live — grep for "payer_address")
  </read_first>
  <behavior>
    - Test 1: Public run response includes `payer_address` redacted as `0x1234...abcd` (6+4) and `payment_signature` as 16-char hex string.
    - Test 2: Internal/admin run shapes still include full `payer_address` and `payment_signature`.
    - Test 3: redactPayer(undefined) returns undefined; redactPayer(short string) returns the original string (no exception); hashSignature(undefined) returns undefined.
  </behavior>
  <action>
    1. Create `~/Projects/x402jobs-api/src/lib/redact.ts`:
       ```ts
       import crypto from 'crypto';

       /** Truncate an address-like string for public display: first 6 + '...' + last 4. */
       export function redactPayer(addr: string | null | undefined): string | null | undefined {
         if (addr == null) return addr;
         if (typeof addr !== 'string') return addr;
         if (addr.length <= 12) return addr; // too short to truncate meaningfully
         return `${addr.slice(0, 6)}...${addr.slice(-4)}`;
       }

       /** Stable opaque digest of a payment signature: SHA-256, first 16 hex chars. */
       export function hashSignature(sig: string | null | undefined): string | null | undefined {
         if (sig == null) return sig;
         if (typeof sig !== 'string' || sig.length === 0) return sig;
         return crypto.createHash('sha256').update(sig).digest('hex').slice(0, 16);
       }
       ```
    2. In `src/routes/runs.ts` (lines 312-405), find the public response mapper. Look for any field assignment like `payer_address: row.payer_address` or `payment_signature: row.payment_signature` inside the handler for the PUBLIC route(s) (NOT the admin/owner routes). Wrap:
       ```ts
       import { redactPayer, hashSignature } from '../lib/redact';
       // ...
       payer_address: redactPayer(row.payer_address),
       payment_signature: hashSignature(row.payment_signature),
       ```
       If the handler returns the raw row, build an explicit projection instead — do not spread the full row to the public response.
    3. In `src/routes/wallet.ts` (lines 289-371), repeat the same pattern for the public wallet activity shape.
    4. **Critical**: identify which routes are public vs admin by checking the middleware applied. Authed-but-not-admin routes still need the redaction (the user viewing the activity is fine seeing their own redacted data; we redact for the consistent open-source API surface). Admin/internal routes that have `requireAdmin` middleware keep the full values.
    5. If the codebase has a separate "admin shape" file or function (grep for `adminShape\|internalShape`), leave it untouched — only modify public response mappers.
  </action>
  <verify>
    <automated>cd ~/Projects/x402jobs-api && test -f src/lib/redact.ts && grep -q "redactPayer" src/routes/runs.ts && grep -q "redactPayer" src/routes/wallet.ts && grep -q "hashSignature" src/routes/runs.ts && grep -q "hashSignature" src/routes/wallet.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd ~/Projects/x402jobs-api && test -f src/lib/redact.ts && echo OK` outputs OK
    - `cd ~/Projects/x402jobs-api && grep -cE "redactPayer\\(|hashSignature\\(" src/routes/runs.ts` outputs ≥ 2
    - `cd ~/Projects/x402jobs-api && grep -cE "redactPayer\\(|hashSignature\\(" src/routes/wallet.ts` outputs ≥ 2
    - `cd ~/Projects/x402jobs-api && grep -cE "export function (redactPayer|hashSignature)" src/lib/redact.ts` outputs `2`
    - `cd ~/Projects/x402jobs-api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>redact.ts created; public run and wallet responses redact payer_address and hash payment_signature; admin/internal shapes untouched.</done>
</task>

<task type="verification">
  <name>Task 5: Add tests for HIGH-04, HIGH-06, HIGH-12</name>
  <files>
    ~/Projects/x402jobs-api/src/routes/__tests__/upload.test.ts
    ~/Projects/x402jobs-api/src/routes/__tests__/escrow.test.ts
    ~/Projects/x402jobs-api/src/routes/__tests__/runs.test.ts
    ~/Projects/x402jobs-api/src/lib/__tests__/redact.test.ts
  </files>
  <read_first>
    - ~/Projects/x402jobs-api/src/routes/upload.ts (final, post Task 2)
    - ~/Projects/x402jobs-api/src/routes/escrow.ts (final, post Task 3)
    - ~/Projects/x402jobs-api/src/routes/runs.ts (final, post Task 4)
    - ~/Projects/x402jobs-api/src/lib/redact.ts (final, post Task 4)
    - Any existing test in src/routes/__tests__/ (to mirror the project's vitest + supertest patterns)
  </read_first>
  <action>
    1. **HIGH-04 test** (`src/routes/__tests__/upload.test.ts`, add or extend):
       - Test "POST /upload/from-url ignores userId from body":
         - Auth as `user-A`.
         - POST body `{ userId: 'user-B-attacker', imageUrl: 'https://example.com/x.png' }`.
         - Assert the resulting DB row (or generateFilePath call argument) uses `user-A`'s id, NOT `user-B-attacker`.
       - If the endpoint can't be E2E tested without S3 mocks, test the path-generation logic directly: extract the `generateFilePath(...)` call and assert it was passed `req.user!.id`.
    2. **HIGH-06 test** (`src/routes/__tests__/escrow.test.ts`, add or extend):
       - Test "escrow webhook rejects wrong-length secret with 401" (passes a 5-char string vs. 32-char expected).
       - Test "escrow webhook rejects same-length-wrong-value secret with 401" (passes a 32-char wrong string).
       - Test "escrow webhook accepts correct secret with 200" (or whatever success status it returns).
       - Test "escrow webhook returns 500 when ESCROW_WEBHOOK_SECRET env unset" (unset env in beforeEach for this test).
    3. **HIGH-12 test** (`src/lib/__tests__/redact.test.ts` for unit tests, plus `src/routes/__tests__/runs.test.ts` for one integration test):
       - `redact.test.ts`: 6 cases:
         - `redactPayer('0x1234567890abcdef1234567890abcdef12345678')` → `'0x1234...5678'`
         - `redactPayer(null)` → `null`
         - `redactPayer(undefined)` → `undefined`
         - `redactPayer('short')` → `'short'` (length ≤ 12, returned as-is)
         - `hashSignature('abc')` returns 16 lowercase hex chars
         - `hashSignature(null)` → `null`
       - `runs.test.ts`: one integration test that GETs a public run endpoint and asserts the JSON has redacted `payer_address` (contains `...`) and 16-char hex `payment_signature`.
    4. Use the existing test framework (vitest + supertest based on baseline). Mirror existing patterns in `__tests__/`.
  </action>
  <verify>
    <automated>cd ~/Projects/x402jobs-api && pnpm vitest run src/routes/__tests__/upload.test.ts src/routes/__tests__/escrow.test.ts src/routes/__tests__/runs.test.ts src/lib/__tests__/redact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd ~/Projects/x402jobs-api && pnpm vitest run src/routes/__tests__/upload.test.ts src/routes/__tests__/escrow.test.ts src/routes/__tests__/runs.test.ts src/lib/__tests__/redact.test.ts` exits 0
    - Each test file's output reports at least the case counts: upload ≥ 2 new, escrow ≥ 4 new, runs ≥ 1 new, redact ≥ 6 new
    - `cd ~/Projects/x402jobs-api && grep -cE "userId.*attacker|attacker.*userId" src/routes/__tests__/upload.test.ts` outputs ≥ 1 (proof the cross-user planting test exists)
  </acceptance_criteria>
  <done>All new tests pass; HIGH-04 cross-user planting case, HIGH-06 four-case secret matrix, and HIGH-12 unit + integration coverage all green.</done>
</task>

<task type="verification">
  <name>Task 6: Baseline regression — full test suite</name>
  <files></files>
  <read_first>
    - /tmp/28-01-baseline.txt (from Task 1)
  </read_first>
  <action>
    1. `cd ~/Projects/x402jobs-api && pnpm tsc --noEmit 2>&1 | tail -5` — typecheck must pass.
    2. `cd ~/Projects/x402jobs-api && pnpm vitest run --exclude '**/resource-registration*' 2>&1 | tail -10 | tee /tmp/28-01-after.txt`.
    3. Extract new test count and compare to baseline; new count MUST be ≥ baseline + (new tests added in Task 5).
    4. If any pre-existing test fails (count regression), STOP. Identify the regression cause before continuing.
  </action>
  <verify>
    <automated>cd ~/Projects/x402jobs-api && pnpm vitest run --exclude '**/resource-registration*' 2>&1 | grep -E "Tests +[0-9]+ (passed|skipped)" | awk '{print $2}' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `cd ~/Projects/x402jobs-api && pnpm tsc --noEmit` exits 0
    - `cd ~/Projects/x402jobs-api && pnpm vitest run --exclude '**/resource-registration*'` exits 0
    - Test count in `/tmp/28-01-after.txt` ≥ test count in `/tmp/28-01-baseline.txt`
    - No "Tests N failed" lines in `/tmp/28-01-after.txt`
  </acceptance_criteria>
  <done>Typecheck clean, full suite green, count not regressed.</done>
</task>

<task type="execute">
  <name>Task 7: Update REVIEW.md status for HIGH-04, HIGH-06, HIGH-12</name>
  <files>.planning/phases/28-security-review/REVIEW.md</files>
  <read_first>
    - .planning/phases/28-security-review/REVIEW.md (the HIGH-04, HIGH-06, HIGH-12 sections at lines 128, 137, 167)
  </read_first>
  <action>
    1. Capture the head commit sha: `cd ~/Projects/x402jobs-api && git rev-parse HEAD > /tmp/28-01-sha.txt` (will be the last fix commit).
    2. Open `.planning/phases/28-security-review/REVIEW.md`.
    3. Under each of the three HIGH sections (HIGH-04 at ~line 128, HIGH-06 at ~line 137, HIGH-12 at ~line 167), append a new line directly after the "**Fix:** ..." line:
       ```
       **Status:** FIXED (commit <sha-from-/tmp/28-01-sha.txt>, plan 28-01)
       ```
    4. Do NOT modify HIGH-01, HIGH-02, HIGH-03, HIGH-05, HIGH-07, HIGH-08, HIGH-09, HIGH-10, HIGH-11, HIGH-13 sections.
    5. Do NOT modify the frontmatter `findings:` counts.
  </action>
  <verify>
    <automated>grep -c "^\\*\\*Status:\\*\\* FIXED" .planning/phases/28-security-review/REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 "^### HIGH-04:" .planning/phases/28-security-review/REVIEW.md | grep -q "Status.*FIXED"` exits 0
    - `grep -A1 "^### HIGH-06:" .planning/phases/28-security-review/REVIEW.md | grep -q "Status.*FIXED"` exits 0
    - `grep -A1 "^### HIGH-12:" .planning/phases/28-security-review/REVIEW.md | grep -q "Status.*FIXED"` exits 0
    - `grep -c "^\\*\\*Status:\\*\\* FIXED" .planning/phases/28-security-review/REVIEW.md` outputs ≥ 3
    - `grep -A1 "^### HIGH-01:" .planning/phases/28-security-review/REVIEW.md | grep -q "Status.*FIXED"` exits 1 (HIGH-01 not yet fixed)
  </acceptance_criteria>
  <done>REVIEW.md HIGH-04, HIGH-06, HIGH-12 marked FIXED with commit reference; other HIGHs untouched.</done>
</task>

</tasks>

<verification>
- run: `cd ~/Projects/x402jobs-api && pnpm tsc --noEmit`
  expect: exit 0
- run: `cd ~/Projects/x402jobs-api && pnpm vitest run --exclude '**/resource-registration*'`
  expect: ≥ baseline test count + 13 (2 upload + 4 escrow + 1 runs + 6 redact); zero failures
- run: `cd ~/Projects/x402jobs-api && grep -cE "const \\{[^}]*userId[^}]*\\}\\s*=\\s*req\\.body" src/routes/upload.ts`
  expect: 0
- run: `cd ~/Projects/x402jobs-api && grep -c "crypto.timingSafeEqual(" src/routes/escrow.ts`
  expect: ≥ 2
- run: `cd ~/Projects/x402jobs-api && grep -cE "redactPayer\\(|hashSignature\\(" src/routes/runs.ts`
  expect: ≥ 2
- run: `cd ~/Projects/x402jobs-api && grep -cE "redactPayer\\(|hashSignature\\(" src/routes/wallet.ts`
  expect: ≥ 2
- run: `grep -c "^\\*\\*Status:\\*\\* FIXED" .planning/phases/28-security-review/REVIEW.md`
  expect: ≥ 3
</verification>

<success_criteria>
- HIGH-04, HIGH-06, HIGH-12 remediated in code with tests.
- Baseline test count preserved; new tests pass.
- REVIEW.md status updated to FIXED for all three findings.
- No regressions in upload, escrow, runs, or wallet flows.
- Branch `fix/high-batch-a-trivial-wins` ready for PR titled `fix(security): HIGH-04/06/12 — Batch A trivial wins`.
</success_criteria>

<output>
After completion, create `.planning/phases/28-security-review/28-01-SUMMARY.md` documenting:
- Commit SHAs for each fix
- Final test count vs. baseline
- Any deviations from the plan
- PR URL
</output>
