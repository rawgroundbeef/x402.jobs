---
phase: 31-monorepo-merge-bsl
plan: 05
type: execute
wave: 4
depends_on: ["31-01", "31-02", "31-03", "31-04"]
files_modified:
  - .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md
  - .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SC1, SC2, SC3, SC4, SC5, SC6, SC7, SC8]
must_haves:
  truths:
    - "Pre-merge: history-audit check confirms `apps/api-audit-tmp/` was never committed (`git log --all -- 'apps/api-audit-tmp/*'` empty per RESEARCH Security Domain row)"
    - "Pre-merge: history-audit check confirms no secrets in the squash-import commit (re-run of plan 02 Task 9 post-commit audit on the final state)"
    - "Pre-merge: history-audit cross-grep proves LICENSE Licensor field is EXACTLY `Memeputer LLC` (T-31-02 verbatim invariant)"
    - "Pre-merge: cross-environment pnpm@10.6.5 pin audit — 6 sites all match (root package.json, apps/web/vercel.json, apps/api/Dockerfile, apps/api/vercel.json if not deleted, .github/workflows/ci.yml, apps/api/package.json#packageManager). Note: apps/api/vercel.json was DELETED by plan 02 — verify ABSENT, not just matching."
    - "Pre-merge: BSL invariants — `LICENSE` contains `Business Source License 1.1`, `Memeputer LLC`, `Memeputer LLC, or its successor`, `Apache License, Version 2.0`, valid Change Date YYYY-MM-DD"
    - "Pre-merge: SECURITY.md externalizes `minimum-release-age=4320` AND mentions both encryption-secret env-vars + has empty Known Unfixed Findings"
    - "Phase 31 PR merged to main (Vercel + Railway deploys triggered)"
    - "Post-merge: Vercel main-branch build log shows `pnpm@10.6.5`, ZERO ENOWORKSPACES, build success (per 30-CONVERGENCE.md 'Expected build-log assertions')"
    - "Post-merge: Railway service re-pointed to `rawgroundbeef/x402.jobs` repo + `apps/api/` root dir (Source: Settings → Service → Source Repository)"
    - "Post-merge: Railway build log shows `pnpm@10.6.5`, ZERO `Ignored build scripts:` (api repo has its own union-empty-after-suppression set per 30-CONVERGENCE.md)"
    - "Post-merge: Railway deployed api boot log does NOT contain `WALLET_ENCRYPTION_SECRET environment variable is not set` (Phase 27 boot guard — proves both encryption secrets survived re-point per Pitfall 4)"
    - "Post-merge: Railway service env vars include BOTH `WALLET_ENCRYPTION_SECRET` and `INTEGRATION_ENCRYPTION_SECRET` (visual verification in Railway UI)"
    - "Post-merge: live https://x402.jobs (Vercel) renders + can make a successful API call to the Railway-hosted api (cross-service smoke)"
    - "Archive ceremony: `rawgroundbeef/x402-jobs-api` GitHub repo is archived (read-only, visibility STILL PRIVATE per RESEARCH Pattern 6 + Pitfall 5 step 8)"
    - "Public flip: `rawgroundbeef/x402.jobs` GitHub repo flipped to PUBLIC visibility (ONLY after all prior steps complete — Pitfall 5 step 9)"
    - "`.planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md` exists with SC1..SC8 mapped to evidence"
    - "`.planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md` exists with EXACT commit shas from plans 31-01/02/03/04 + Railway-revert procedure + visibility-flip-back procedure"
    - "`.planning/STATE.md` updated: Phase 31 marked complete, v3.0 progress advanced, Session Continuity points at Phase 32"
    - "`.planning/ROADMAP.md` updated: Phase 31 plan checklist all `[x]` + Phase 31 entry in Progress table marked Complete"
  artifacts:
    - path: ".planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md"
      provides: "Cross-environment convergence evidence + Phase 31 ROADMAP success-criteria sign-off"
    - path: ".planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md"
      provides: "Single-page rollback runbook with commit shas, Railway re-point procedure, public-flip-reversal"
    - path: ".planning/STATE.md"
      provides: "Phase 31 marked complete; v3.0 progress counter advanced; Session Continuity → Phase 32"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 31 entry shows all plans complete; Progress table updated"
  key_links:
    - from: "Vercel main deploy log"
      to: "30-CONVERGENCE.md 'Expected build-log assertions'"
      via: "pnpm@10.6.5 + zero ENOWORKSPACES + zero unexpected Ignored scripts"
      pattern: "pnpm@10\\.6\\.5"
    - from: "Railway main deploy log"
      to: "30-CONVERGENCE.md 'Expected build-log assertions' (api side)"
      via: "pnpm@10.6.5 + zero Ignored build scripts + isolated-vm suppressed"
      pattern: "pnpm@10\\.6\\.5"
    - from: "Public flip ceremony"
      to: "RESEARCH Pitfall 5 strict order (docs→import→CI→smoke→Railway→Vercel→merge→archive→public)"
      via: "Step-by-step checklist in Task 6, each step gated by prior completion"
      pattern: "public"
    - from: "31-ROLLBACK.md"
      to: "All 4 prior plan commit shas + Railway revert + visibility-flip-back"
      via: "Captures actual shas captured by plans 01/02/03/04 SUMMARYs"
      pattern: "[0-9a-f]{7,40}"
---

<objective>
The Phase 31 closure plan. Audit the pre-merge state (history, licensor, pnpm pin convergence, BSL invariants, SECURITY.md externalizations), merge the Phase 31 PR to `main`, watch Vercel + Railway deploy on `main` (NOT pre-merge), re-point Railway service to the new repo + root dir (Pitfall 4 — verify both encryption secrets survive), verify the post-merge build logs match 30-CONVERGENCE.md "Expected build-log assertions", archive the old `rawgroundbeef/x402-jobs-api` GitHub repo (read-only, stays private), flip `rawgroundbeef/x402.jobs` to PUBLIC visibility (last step — Pitfall 5 strict order), and produce the convergence + rollback docs + STATE.md/ROADMAP.md updates.

Purpose: The four prior plans landed independent edits. This plan is the goal-backward verification that the 8 ROADMAP Phase 31 SCs are all satisfied. Without this plan, prior commits are disconnected — public-flip risks exposing broken state. The strict ceremony order (RESEARCH Pitfall 5) ensures: docs → import → CI → smoke → Railway re-point → Vercel verify → merge → archive api repo → public flip LAST.

Output: 2 new docs (31-CONVERGENCE.md + 31-ROLLBACK.md), STATE.md/ROADMAP.md updates, final merge approval, Railway re-point executed, api repo archived, public-flip executed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-monorepo-merge-bsl/31-CONTEXT.md
@.planning/phases/31-monorepo-merge-bsl/31-RESEARCH.md
@.planning/phases/31-monorepo-merge-bsl/31-PATTERNS.md
@.planning/phases/30-supply-chain-hardening/30-CONVERGENCE.md
@.planning/phases/30-supply-chain-hardening/30-ROLLBACK.md
@.planning/phases/31-monorepo-merge-bsl/31-01-PLAN.md
@.planning/phases/31-monorepo-merge-bsl/31-02-PLAN.md
@.planning/phases/31-monorepo-merge-bsl/31-03-PLAN.md
@.planning/phases/31-monorepo-merge-bsl/31-04-PLAN.md
</context>

<interfaces>
**ROADMAP Phase 31 Success Criteria → Plan-and-Evidence Map:**

| SC# | Criterion | Verifying Plan(s) | Evidence Source |
|-----|-----------|-------------------|-----------------|
| SC1 | Single repo, single license, single CI | 31-01 (license), 31-02 (single repo), 31-03 (single CI) | LICENSE exists; apps/api/ exists; .github/workflows/ci.yml exists |
| SC2 | Both apps deploy cleanly to Vercel + Railway post-merge | 31-05 (this plan) | Task 4 watches Vercel main deploy; Task 5 watches Railway main deploy |
| SC3 | New clone-and-run dev: `pnpm install && pnpm dev` works | 31-02 Task 10 + 31-04 Task 3 | Plan 02 verified unified path; Plan 04 verified per-app fallbacks |
| SC4 | LICENSE, README, SECURITY.md, CONTRIBUTING.md present | 31-01 | All 5 files committed in plan 01 commit; verified in plan 01 grep gates |
| SC5 | LICENSE names **Memeputer LLC** with locked grant text | 31-01 | Plan 01 Task 1 grep gates `Memeputer LLC, or its successor` + verbatim grant text |
| SC6 | SECURITY.md release-age policy + private disclosure + empty Known unfixed | 31-01 | Plan 01 Task 2 grep gates `minimum-release-age=4320` + `None at public launch` |
| SC7 | SECURITY.md externalizes release-age policy | 31-01 | Same as SC6 |
| SC8 | Vercel + Railway main-branch deploys satisfy 30-CONVERGENCE.md assertions | 31-05 (this plan) | Task 4 + Task 5 inspect deploy logs against 30-CONVERGENCE.md "Expected build-log assertions" |

**Pre-merge invariant cross-grep table (Task 1 enforces):**

| Invariant | Source | Grep gate |
|-----------|--------|-----------|
| LICENSE Licensor = Memeputer LLC (T-31-02) | CONTEXT D-02 | `grep -c '^Licensor: *Memeputer LLC$' LICENSE` = 1 |
| LICENSE grant text verbatim | CONTEXT D-02 | `grep -c 'Memeputer LLC, or its successor' LICENSE` = 1 |
| LICENSE Change License | CONTEXT D-02 | `grep -c '^Change License: *Apache License, Version 2.0$' LICENSE` = 1 |
| SECURITY.md externalizes Phase 30 policy | SC7 | `grep -c 'minimum-release-age=4320' SECURITY.md` ≥ 1 |
| SECURITY.md mentions both encryption secrets | SECURITY.md content | `grep -c 'WALLET_ENCRYPTION_SECRET' SECURITY.md` ≥ 1 AND `grep -c 'INTEGRATION_ENCRYPTION_SECRET' SECURITY.md` ≥ 1 |
| SECURITY.md Known Unfixed empty | CONTEXT D-01 | `grep -c 'None at public launch' SECURITY.md` ≥ 1 |
| README has no MIT | SC4 | `! grep -E '^MIT$\|License: MIT' README.md` |
| pnpm@10.6.5 across 5 sites | 30-CONVERGENCE.md | `grep -rh 'pnpm@10\.6\.5' package.json apps/web/vercel.json apps/api/Dockerfile apps/api/package.json .github/workflows/ci.yml \| wc -l` ≥ 5 |
| No `pnpm@9` stragglers | 30-CONVERGENCE.md SC1 | `git ls-files \| grep -vE '^\.planning/' \| xargs grep -l 'pnpm@9' \| head` = empty |
| No `apps/api-audit-tmp` references | T-31-06 | `git ls-files \| xargs grep -l 'api-audit-tmp' 2>/dev/null \| head` = empty |
| apps/api/vercel.json deleted | D-04 | `test ! -f apps/api/vercel.json` |
| ignoredBuiltDependencies: isolated-vm at root | Pitfall 1 | `grep -q 'isolated-vm' pnpm-workspace.yaml` |

**History audit (T-31-01 + T-31-08 mitigation):**

```bash
# Confirm no secrets in any commit on this branch
git log -p --all -- apps/api/ 2>/dev/null | grep -iE "BEGIN .* PRIVATE KEY|wallet-backup|HELIUS_WEBHOOK_SECRET=.*[a-zA-Z0-9]{20,}|service_role_key.*=.*eyJ" | head | tee /tmp/31-05-history-audit-secrets.txt
test ! -s /tmp/31-05-history-audit-secrets.txt && echo "OK: no secrets in history"

# Confirm apps/api-audit-tmp/ never landed (T-31-06)
git log --all -- 'apps/api-audit-tmp/*' | head | tee /tmp/31-05-history-audit-audit-tmp.txt
test ! -s /tmp/31-05-history-audit-audit-tmp.txt && echo "OK: apps/api-audit-tmp never committed"
```

**Railway re-point procedure (RESEARCH Runtime State Inventory line 549 + Pitfall 4):**

The Railway service that today deploys from `rawgroundbeef/x402-jobs-api` must be re-pointed to `rawgroundbeef/x402.jobs` + root dir `apps/api/`. The encryption secrets MUST survive (LOAD-BEARING — losing them makes every wallet/token unrecoverable).

Recommended path (per RESEARCH Assumption A3 — re-point, do NOT re-create):
1. Open Railway dashboard for the api service.
2. Settings → Service → Source → Source Repository → click "Change repository".
3. Authenticate / select `rawgroundbeef/x402.jobs` (formerly authorized for the api repo; same Memeputer LLC org account).
4. Set Root Directory = `apps/api`.
5. Confirm Dockerfile path = `apps/api/Dockerfile` (Railway should auto-detect).
6. Before saving the change, navigate to Variables tab in a separate browser tab. Visually confirm BOTH `WALLET_ENCRYPTION_SECRET` and `INTEGRATION_ENCRYPTION_SECRET` are still set with non-empty values. If either is missing or empty, STOP — do NOT save the source change.
7. Save source change. Railway will trigger a new build from the new repo path.
8. Watch the build log for `pnpm@10.6.5` + zero `Ignored build scripts` (the api allow-list union from plan 02).
9. Once the new build deploys, watch the api boot log. The Phase 27 boot guard is the load-bearing signal:
   - DOES boot successfully → both encryption secrets present, re-point clean.
   - Boot log says `WALLET_ENCRYPTION_SECRET environment variable is not set` → STOP, revert Railway re-point, re-paste the secret from the prior service config + 1Password backup.
10. Smoke-test a wallet decryption flow (e.g. invoke an authenticated endpoint that reads a wallet) to confirm `INTEGRATION_ENCRYPTION_SECRET` also survived.

**Visibility flip (Pitfall 5 strict order — LAST step):**

```
gh repo edit rawgroundbeef/x402.jobs --visibility public --accept-visibility-change-consequences
# OR via web UI: Settings → General → Danger Zone → Change repository visibility → Make public
```

Before flipping, verify:
- The merge PR is MERGED to main.
- Vercel deploy on main is green.
- Railway re-point complete + boot guard passed.
- `rawgroundbeef/x402-jobs-api` is ARCHIVED (not deleted; stays private).
- All grep-gate audits in Task 1 + Task 2 pass.
- 31-CONVERGENCE.md + 31-ROLLBACK.md are committed.

**Archive ceremony for the old api repo (RESEARCH Pattern 6 lines 506-515):**

```
# Open https://github.com/rawgroundbeef/x402-jobs-api/settings
# Scroll to Danger Zone → Archive this repository → click → confirm
```

Effects (per GitHub Docs verified in RESEARCH):
- Repo becomes read-only (no new issues, no pushes, no merges).
- VISIBILITY UNCHANGED — stays PRIVATE per Pitfall 5 ("Archived api repo accidentally made public").
- Full commit history preserved for legitimate-need reference.
- A final README banner update (optional) can point to the merged repo.

**STATE.md update target (Task 7):**

Current frontmatter:
```yaml
progress:
  total_phases: 9
  completed_phases: 8
  total_plans: 16
  completed_plans: 15
  percent: 94
```

After Phase 31 (5 plans):
```yaml
progress:
  total_phases: 10
  completed_phases: 9
  total_plans: 21
  completed_plans: 20
  percent: 95
```

Also: `stopped_at`, `last_updated`, `last_activity`, Current Position, v3.0 progress bar, Session Continuity, Milestone History trailer.

**ROADMAP.md update target (Task 7):**

Phase 31 section's `Plans:` block — change all 5 entries from `[ ]` to `[x]`. Add `## Progress` table entry: Phase 31 status = Complete, plans = 5/5, requirements = SC1..SC8.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-merge cross-grep audit — BSL invariants, pnpm convergence, no-secret-history</name>
  <files>(no file edits — captures /tmp/31-05-* audit artifacts)</files>
  <action>
    Working dir: `/Users/rawgroundbeef/Projects/x402jobs`. This task runs AFTER plans 01-04 are all on the same branch (or in parallel branches that get rebased onto each other) and BEFORE the PR is merged.

    Verify all four prior plans landed:
    ```bash
    git log --oneline main..HEAD | head -10
    # Expected commits (order may vary):
    #   <31-04-sha> feat(31): add dev:web / dev:api / dev:inngest fallback scripts + test alias
    #   <31-03-sha> feat(31): add unified GitHub Actions CI workflow with dorny/paths-filter@v3
    #   <31-02-sha> feat(monorepo): import x402jobs-api at <sha> into apps/api/
    #   <31-01-sha> docs(31): add BSL 1.1 LICENSE + public-facing docs
    ```

    Step 1: BSL invariant grep gates (T-31-02 mitigation):
    ```bash
    [ "$(grep -c '^Licensor: *Memeputer LLC$' LICENSE)" = "1" ] && echo "OK: SC5 Licensor verbatim"
    [ "$(grep -c 'Memeputer LLC, or its successor' LICENSE)" = "1" ] && echo "OK: SC5 grant verbatim"
    [ "$(grep -c '^Change License: *Apache License, Version 2.0$' LICENSE)" = "1" ] && echo "OK: SC5 Change License"
    grep -q "Copyright © 2026 Memeputer LLC. All rights reserved." LICENSE && echo "OK: SC5 copyright header"
    grep -q "^Terms$" LICENSE && echo "OK: BSL body marker (covenant 4 — body unmodified)"
    grep -qE "^Change Date: *[0-9]{4}-[0-9]{2}-[0-9]{2}" LICENSE && echo "OK: Change Date ISO format"
    ```

    Step 2: SECURITY.md externalization gates (SC6, SC7):
    ```bash
    grep -q "minimum-release-age=4320" SECURITY.md && echo "OK: SC7 release-age externalized"
    grep -q "WALLET_ENCRYPTION_SECRET" SECURITY.md && echo "OK: wallet encryption secret documented"
    grep -q "INTEGRATION_ENCRYPTION_SECRET" SECURITY.md && echo "OK: integration encryption secret documented"
    grep -q "security/advisories/new" SECURITY.md && echo "OK: GitHub PVR link"
    grep -q "None at public launch" SECURITY.md && echo "OK: SC6 empty Known Unfixed"
    # Critical: no HIGHs listed as OPEN
    if grep -E "HIGH-(01|02|03|04|06|07|08|09|10|11|12|13)" SECURITY.md | grep -qi "open"; then
      echo "FAIL: SECURITY.md lists a HIGH as OPEN — contradicts D-01"
      exit 1
    fi
    echo "OK: no HIGHs listed as OPEN in SECURITY.md"
    ```

    Step 3: README has no MIT (SC4):
    ```bash
    ! grep -qE "^MIT$|License: MIT" README.md && echo "OK: README has no MIT"
    grep -q "BSL 1.1" README.md && echo "OK: README references BSL 1.1"
    grep -q "apps/api" README.md && echo "OK: README structure block has apps/api"
    grep -q "CONTRIBUTING.md" README.md && grep -q "SECURITY.md" README.md && echo "OK: README cross-links"
    ```

    Step 4: All 4 public docs + CLAUDE.md exist (SC4):
    ```bash
    for f in LICENSE README.md SECURITY.md CONTRIBUTING.md CLAUDE.md; do
      test -f "$f" && echo "OK: $f present" || echo "FAIL: $f MISSING"
    done
    ```

    Step 5: pnpm@10.6.5 cross-environment pin convergence audit:
    ```bash
    # Capture all sites referencing pnpm@10.6.5 across tracked files
    git ls-files | xargs grep -l "pnpm@10\.6\.5" 2>/dev/null | tee /tmp/31-05-pin-sites.txt
    # Expected (5 sites, apps/api/vercel.json is DELETED per D-04):
    #   .github/workflows/ci.yml
    #   apps/api/Dockerfile
    #   apps/api/package.json
    #   apps/web/vercel.json
    #   package.json
    [ "$(wc -l < /tmp/31-05-pin-sites.txt)" -ge 5 ] && echo "OK: 5+ pnpm@10.6.5 pin sites"

    # No pnpm@9 stragglers in non-planning files (30-CONVERGENCE.md SC1 invariant)
    git ls-files | grep -vE '^\.planning/' | xargs grep -l "pnpm@9" 2>/dev/null | tee /tmp/31-05-pnpm9-stragglers.txt
    test ! -s /tmp/31-05-pnpm9-stragglers.txt && echo "OK: no pnpm@9 stragglers in code"

    # apps/api/vercel.json is DELETED (D-04 per plan 02 task 4)
    test ! -f apps/api/vercel.json && echo "OK: apps/api/vercel.json deleted"

    # No apps/api-audit-tmp references anywhere
    git ls-files | xargs grep -l "api-audit-tmp" 2>/dev/null | tee /tmp/31-05-audit-tmp-refs.txt
    test ! -s /tmp/31-05-audit-tmp-refs.txt && echo "OK: no api-audit-tmp references"
    ```

    Step 6: isolated-vm invariant preserved (Pitfall 1):
    ```bash
    grep -q "ignoredBuiltDependencies:" pnpm-workspace.yaml && grep -q "- isolated-vm" pnpm-workspace.yaml && echo "OK: isolated-vm invariant at root workspace yaml"
    ```

    Step 7: 8-entry union allow-list (30-CONVERGENCE.md:53-61):
    ```bash
    [ "$(node -e "console.log(require('./package.json').pnpm.onlyBuiltDependencies.length)")" = "8" ] && echo "OK: 8-entry union allow-list"
    ```

    Step 8: History audit — no secrets in any commit (T-31-01 + T-31-08):
    ```bash
    git log -p --all -- apps/api/ 2>/dev/null | grep -iE "BEGIN .* PRIVATE KEY|wallet-backup|HELIUS_WEBHOOK_SECRET=.*[a-zA-Z0-9]{20,}|service_role_key.*=.*eyJ[a-zA-Z0-9]" | head | tee /tmp/31-05-history-audit-secrets.txt
    test ! -s /tmp/31-05-history-audit-secrets.txt && echo "OK: no secrets in history"

    # Confirm apps/api-audit-tmp/ never had a commit (RESEARCH Security Domain row)
    git log --all -- 'apps/api-audit-tmp/*' | head | tee /tmp/31-05-history-audit-audit-tmp.txt
    test ! -s /tmp/31-05-history-audit-audit-tmp.txt && echo "OK: apps/api-audit-tmp never committed"
    ```

    Step 9: CI workflow file exists and is valid YAML (Plan 03 sanity check):
    ```bash
    test -f .github/workflows/ci.yml && echo "OK: CI workflow present"
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml').read())" && echo "OK: CI YAML parseable"
    grep -q "dorny/paths-filter@v3" .github/workflows/ci.yml && echo "OK: dorny/paths-filter@v3"
    grep -q "version: 10.6.5" .github/workflows/ci.yml && echo "OK: workflow pins pnpm@10.6.5"
    ```

    All OK lines must print. If ANY fails, STOP and surface to the user before proceeding to merge.
  </action>
  <verify>
    <automated>cd /Users/rawgroundbeef/Projects/x402jobs && [ "$(grep -c '^Licensor: *Memeputer LLC$' LICENSE)" = "1" ] && grep -q "Memeputer LLC, or its successor" LICENSE && grep -q "minimum-release-age=4320" SECURITY.md && grep -q "None at public launch" SECURITY.md && ! grep -qE "^MIT$|License: MIT" README.md && test -f LICENSE && test -f SECURITY.md && test -f CONTRIBUTING.md && test -f CLAUDE.md && [ "$(git ls-files | xargs grep -l 'pnpm@10\.6\.5' 2>/dev/null | wc -l)" -ge 5 ] && test ! -f apps/api/vercel.json && test ! -s /tmp/31-05-history-audit-secrets.txt && test ! -s /tmp/31-05-history-audit-audit-tmp.txt && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml').read())"</automated>
  </verify>
  <done>
    - LICENSE Licensor + grant text verbatim (SC5)
    - SECURITY.md externalizes release-age policy + has empty Known Unfixed (SC6, SC7)
    - README has no MIT, links to LICENSE/CONTRIBUTING/SECURITY (SC4)
    - All 5 public docs present (SC4)
    - pnpm@10.6.5 pinned in 5+ sites; no pnpm@9 stragglers in code
    - apps/api/vercel.json deleted; no apps/api-audit-tmp references
    - isolated-vm invariant at root workspace yaml
    - 8-entry union allow-list
    - History audit clean: no secrets ever committed; apps/api-audit-tmp never tracked
    - CI workflow valid YAML with dorny/paths-filter@v3 + pnpm 10.6.5 pin
    - Captured audit artifacts in /tmp/31-05-* for SUMMARY
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final pre-merge PR review — confirm CI green, LICENSE preview, SECURITY.md readable</name>
  <what-built>
    The Phase 31 PR contains all four prior plans' commits. Plan 01's docs are review-able as text. Plan 02's squash-import is review-able as one merge commit (with file count visible in the GitHub diff). Plan 03's CI workflow is running on the PR right now. Plan 04's package.json scripts are review-able as a 4-line diff.
  </what-built>
  <how-to-verify>
    1. Open the Phase 31 PR:
       ```bash
       gh pr view
       gh pr checks   # should show changes / web / api jobs all passing
       ```

    2. PR checks:
       - All CI jobs are GREEN (`changes`, `web`, `api`).
       - No conflict markers shown in any diff.
       - Diff is bounded to: 5 docs files (Plan 01) + apps/api/ + 4 root config files + .gitignore (Plan 02) + .github/workflows/ci.yml (Plan 03) + package.json scripts diff (Plan 04).
       - NO unexpected files (no test scaffolds, no random tooling, no .env files, no wallet-backup files).

    3. Inspect the LICENSE diff in the PR:
       - GitHub UI may render LICENSE specially in the right sidebar (auto-detected BSL 1.1?). Either way, the diff should show the complete file with `Licensor: Memeputer LLC`, `Memeputer LLC, or its successor`, `Apache License, Version 2.0`, valid Change Date.

    4. Inspect the SECURITY.md diff:
       - GitHub will surface SECURITY.md in the Security tab once merged. Pre-merge, the diff itself should show: GitHub PVR link, `minimum-release-age=4320`, both encryption-secret env-var names, `None at public launch.` in the Known Unfixed section.

    5. Inspect the squash-import commit (Plan 02 Task 9):
       - Commit subject: `feat(monorepo): import x402jobs-api at <sha> into apps/api/`.
       - Commit body references SOURCE sha + archived repo URL + Phase 28 Batch H carry-forward.
       - Diff shows 100+ files added under apps/api/ + 4 root tooling files modified + .gitignore modified.

    6. Inspect the CI workflow diff (Plan 03):
       - File path: `.github/workflows/ci.yml` (NOT under apps/api/).
       - Contains `dorny/paths-filter@v3` + `version: 10.6.5` + `permissions: pull-requests: read`.

    7. Inspect the package.json scripts diff (Plan 04):
       - 4 new entries: `dev:web`, `dev:api`, `dev:inngest`, `test`.
       - 5 existing scripts (`build`, `dev`, `lint`, `typecheck`, `clean`) UNCHANGED.

    8. Hit the live preview from CI (if any deploy preview is wired — not expected since Railway has no per-PR previews and Vercel preview gating was opted out per memory `feedback_deploy_lane_validation.md`). If a Vercel preview URL is available from CI artifacts, click through and confirm the web app renders.

    9. If all checks pass → reply `approved-to-merge`. DO NOT actually merge yet — Task 3 documents the merge step + post-merge gates.
  </how-to-verify>
  <resume-signal>
    Type `approved-to-merge` to proceed to merge + Railway re-point + ceremony. OR describe any pre-merge concern (CI failure, license wording issue, unexpected file in diff).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge the Phase 31 PR to main (triggers Vercel deploy; Railway will need re-point separately)</name>
  <files>(GitHub merge action; no local file edits)</files>
  <action>
    Working dir: `/Users/rawgroundbeef/Projects/x402jobs`.

    Pre-merge final check (idempotent re-run of Task 1's gates — fast safety net):
    ```bash
    cd /Users/rawgroundbeef/Projects/x402jobs
    grep -q "Memeputer LLC" LICENSE && grep -q "minimum-release-age=4320" SECURITY.md && ! grep -qE "^MIT$|License: MIT" README.md && test -d apps/api && test -f .github/workflows/ci.yml && echo "OK: pre-merge invariants intact"
    ```

    Capture commit shas of all 4 prior plans (for 31-ROLLBACK.md in Task 5):
    ```bash
    git log --oneline main..HEAD | tee /tmp/31-05-branch-commits.txt
    # Capture individual shas (assumes 4 plan commits):
    git log --pretty="%H %s" main..HEAD | tee /tmp/31-05-branch-shas.txt
    ```

    Merge the PR. Recommended merge method: **squash-and-merge** (combines all 4 plan commits + the per-plan SUMMARY commits into one main commit). This matches Phase 30's convention; the per-plan history is preserved in the closed branch and in the PR view.

    ```bash
    # Replace <pr-number> with the actual Phase 31 PR number
    PR_NUMBER=$(gh pr view --json number -q '.number')
    gh pr merge --squash --delete-branch ${PR_NUMBER}
    # OR via web UI: PR page → Merge → Squash and merge → confirm → Delete branch
    ```

    Capture the merge commit sha on main:
    ```bash
    git checkout main && git pull
    git log --oneline -1 | tee /tmp/31-05-merge-commit.txt
    MERGE_SHA=$(git rev-parse HEAD)
    echo "Phase 31 main merge sha: ${MERGE_SHA}"
    ```

    Confirm Vercel auto-deploy starts:
    ```bash
    # Watch with vercel CLI if available, or via Vercel dashboard:
    # The deploy lane is rawgroundbeef/x402.jobs → Vercel project; install command pins pnpm@10.6.5.
    # Per 30-CONVERGENCE.md "Expected build-log assertions":
    #   - pnpm@10.6.5 is installed
    #   - 0 ENOWORKSPACES
    #   - 5 web-only-accepted Ignored build scripts (BLOCKER-30-01-A) — present but accepted
    #   - 48/48 pages built
    ```

    Note: Railway's auto-deploy will NOT fire from this merge because Railway is still pointed at the SIBLING `rawgroundbeef/x402-jobs-api` repo. Railway re-point happens in Task 4.
  </action>
  <verify>
    <automated>cd /Users/rawgroundbeef/Projects/x402jobs && git checkout main && git pull && git log --oneline -1 | grep -qE "Phase 31|monorepo merge|BSL"</automated>
  </verify>
  <done>
    - Pre-merge gates re-verified
    - 4 plan commit shas captured to /tmp/31-05-branch-shas.txt (for 31-ROLLBACK.md)
    - PR merged to main via squash-and-merge
    - Branch deleted
    - Local main updated; merge sha captured
    - Vercel main-branch deploy starts (Task 4 watches it)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vercel main-branch deploy verification (SC2 + SC5 + SC8 web side)</name>
  <what-built>
    Merging Phase 31 to main triggered Vercel to build from `apps/web/vercel.json#buildCommand` against the new merged tree. The post-merge build must match 30-CONVERGENCE.md "Expected build-log assertions" for the web side.
  </what-built>
  <how-to-verify>
    1. Open Vercel dashboard for the `rawgroundbeef/x402.jobs` web project. Navigate to the deployment triggered by the merge commit.

    2. Inspect the build log. Required signals (per 30-CONVERGENCE.md):
       - `npm install -g pnpm@10.6.5 && pnpm install --frozen-lockfile` step succeeds.
       - Install log shows `Successfully installed pnpm@10.6.5` — NOT `pnpm@10.7.0+`, NOT floating `pnpm@10`.
       - Install log has ZERO `ERR_PNPM_NO_MATCHING_VERSION` lines.
       - Install log has the 5 BLOCKER-30-01-A accepted `Ignored build scripts:` lines (`@stellar/stellar-sdk`, `blake-hash`, `tiny-secp256k1`, `unrs-resolver`, `usb`) — NO new package names.
       - Build step runs `turbo run build --filter=@x402jobs/web...` and completes successfully.
       - Next.js build output reports 48/48 pages built (or current count if the resource-registration redesign added more) with ZERO `ENOWORKSPACES` errors.

    3. Open the production URL (`https://x402.jobs`) — confirm the web app renders.

    4. Click into 2-3 routes (creator dashboard, resources index, a resource detail page) — confirm pages load. Note: the api is STILL pointing at the old Railway service at this point (Task 5 re-points it), so API calls may go to the existing api deployment. That's expected — Vercel + Railway are independent lanes.

    5. If ALL signals match expectations → reply `approved`.

    Failure modes specific to this checkpoint:
    - `pnpm@10.7.0` installed instead of `10.6.5`: drift somewhere — check `apps/web/vercel.json#installCommand` for accidental float; re-verify package.json#packageManager.
    - `ENOWORKSPACES` appears: the pnpm 10.6.5 pin failed to hold; check that no upstream Vercel auto-bump happened. Forward-fix per 30-ROLLBACK.md Symptom A.
    - `ERR_PNPM_NO_MATCHING_VERSION` for some dep: a transitive in the lockfile is now <72h old (e.g. a security patch). Forward-fix per 30-ROLLBACK.md Symptom D: add `minimum-release-age-exclude=<pkg-name>` to `.npmrc`, push.
    - New `Ignored build scripts:` package not in the BLOCKER-30-01-A set: an upstream dep added a postinstall step. Forward-fix per 30-ROLLBACK.md Symptom E: add to `pnpm.onlyBuiltDependencies` after verifying legitimacy.

    DO NOT proceed to Task 5 (Railway re-point) if Vercel main-deploy is failing. The web app stays on whatever last-green deploy is active; Vercel does not auto-roll-back. Vercel rollback procedure: dashboard → Deployments → previous deployment → Promote.
  </how-to-verify>
  <resume-signal>
    Type `approved` if Vercel main-deploy succeeds per 30-CONVERGENCE.md assertions, OR describe the failure (log snippet + which assertion failed).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Railway service re-point + encryption-secret survival verification (SC2 + SC4 + SC8 api side; Pitfall 4 + T-31-05)</name>
  <what-built>
    The Railway service for the api still points at `rawgroundbeef/x402-jobs-api` (root dir). Now we re-point it to `rawgroundbeef/x402.jobs` + root dir `apps/api/`. The CRITICAL invariant is that `WALLET_ENCRYPTION_SECRET` and `INTEGRATION_ENCRYPTION_SECRET` survive — losing either breaks every user wallet/token permanently.
  </what-built>
  <how-to-verify>
    PRE-FLIGHT (do NOT skip):

    1. Have BOTH encryption secrets accessible from a backup (1Password) so you can verify their current values match what's in Railway.

    2. Take note of any other Railway env vars currently set on the api service (Supabase URL/keys, Helius webhook secret, Twitter API keys, JWT secrets, etc). Re-pointing should preserve all of them, but having a list lets you cross-check.

    RE-POINT EXECUTION:

    3. Open Railway dashboard → x402-jobs-api service → Settings tab.

    4. Before changing Source: open the Variables tab in a SEPARATE BROWSER WINDOW. Visually confirm `WALLET_ENCRYPTION_SECRET` and `INTEGRATION_ENCRYPTION_SECRET` are both set with non-empty values. Match against your 1Password backup. If either is missing or empty, STOP — do not proceed. Fix the env var first.

    5. In the Settings tab: Source → Source Repository → click "Change repository".

    6. Authenticate / select `rawgroundbeef/x402.jobs`. (The Memeputer LLC org may need to re-grant GitHub app access; if so, do that first.)

    7. Set Root Directory = `apps/api`. Railway should auto-detect the Dockerfile at `apps/api/Dockerfile`.

    8. Verify the Variables tab is STILL populated (env vars are service-scoped, not repo-scoped — they should survive a source change. RESEARCH Assumption A3.). Re-confirm both encryption secrets are present.

    9. Save the source change.

    POST-RE-POINT VERIFICATION:

    10. Railway auto-triggers a new build from the new path. Open the build log.

    11. Build log invariants (per 30-CONVERGENCE.md "Expected build-log assertions" — api side):
        - Dockerfile is `apps/api/Dockerfile`.
        - `RUN npm install -g pnpm@10.6.5` step succeeds (NOT pnpm@9, NOT floating `pnpm@10`).
        - `pnpm install --frozen-lockfile` succeeds with ZERO `ERR_PNPM_NO_MATCHING_VERSION` and ZERO unexpected `Ignored build scripts:` (api's allow-list union per 30-CONVERGENCE.md line 53 is empirically empty-after-suppression).
        - `pnpm build` runs `tsup` and produces `apps/api/dist/index.js` (~880KB per 30-CONVERGENCE.md baseline).
        - Container starts; healthcheck passes (or Railway shows the service as Active).

    12. Critical: open the runtime log (NOT build log). The api's boot guard from Phase 27 is the load-bearing signal. Search for:
        - `WALLET_ENCRYPTION_SECRET environment variable is not set` → STOP, revert Railway source change, re-paste secret from 1Password.
        - `INTEGRATION_ENCRYPTION_SECRET environment variable is not set` → same.
        - Healthy boot: typically `API listening on port 3011` or similar, NO env-not-set warnings.

    13. End-to-end smoke (cross-service):
        ```bash
        curl -s -o /dev/null -w "%{http_code}\n" https://x402.jobs    # 200/307
        # Then click through to a route on x402.jobs that triggers an API call to Railway-api.
        # E.g. log in, view a resource that requires decrypting a wallet.
        # The successful page load proves WALLET_ENCRYPTION_SECRET round-trips.
        # The successful Twitter integration display (if you have one connected) proves INTEGRATION_ENCRYPTION_SECRET round-trips.
        ```

    14. Capture for SUMMARY:
        - The new Railway deployment URL/ID.
        - The pnpm version line from the build log (must say 10.6.5).
        - Confirmation that no `WALLET_ENCRYPTION_SECRET environment variable is not set` line appeared in boot log.

    15. If ALL of the above pass → reply `approved`.

    Rollback path if verification fails:
    - Revert Railway source change: Settings → Source → Change repository → back to `rawgroundbeef/x402-jobs-api` (still archived but private — Railway can deploy from archived repos for the lifetime of an existing PAT/integration).
    - Re-deploy from the archived api repo.
    - File a bug report + investigate the failure mode before retrying.

    DO NOT proceed to Task 6 (archive ceremony + public flip) if Railway re-point fails. The api repo must stay un-archived AND private until Railway is happily deployed from the new lane.
  </how-to-verify>
  <resume-signal>
    Type `approved` if Railway re-point succeeds AND both encryption secrets survived AND build log matches 30-CONVERGENCE.md assertions. OR describe what failed.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Archive ceremony + PUBLIC visibility flip (Pitfall 5 strict order, LAST step)</name>
  <what-built>
    With Vercel + Railway both deploying clean from the merged repo, the old `rawgroundbeef/x402-jobs-api` GitHub repo is ready to be archived (read-only, STAYS PRIVATE). Then — and ONLY then — `rawgroundbeef/x402.jobs` flips to PUBLIC visibility. This is the irreversible step in the cosmic sense (technically reversible, but visibility-flip-back is a confusing signal to early adopters).
  </what-built>
  <how-to-verify>
    PRE-FLIGHT CHECKLIST (RESEARCH Pitfall 5 strict order — every step MUST be true before proceeding):

    - [ ] Plan 01 (LICENSE + SECURITY.md + CONTRIBUTING.md + CLAUDE.md + README.md) merged.
    - [ ] Plan 02 (squash-import) merged.
    - [ ] Plan 03 (CI workflow) merged.
    - [ ] Plan 04 (dev fallback scripts) merged.
    - [ ] Task 1 cross-grep audit passed.
    - [ ] Task 2 PR pre-merge review approved.
    - [ ] Task 3 merge to main complete.
    - [ ] Task 4 Vercel main deploy green per 30-CONVERGENCE.md.
    - [ ] Task 5 Railway re-point complete, both encryption secrets survived, boot guard clean, cross-service smoke passed.

    If ANY checkbox is unchecked, STOP. Address it first. Public-flip on partial state would expose the repo in a broken configuration.

    STEP A: Update the api repo's README with a final banner pointing to the merged repo (per RESEARCH Pattern 6 step 1; optional but recommended):

    ```bash
    cd ~/Projects/x402jobs-api
    git checkout main && git pull
    # Edit README.md — add at top:
    #   > **This repo is now archived. The code lives at https://github.com/rawgroundbeef/x402.jobs**
    #   > **under `apps/api/`. See https://x402.jobs for the production deployment.**
    git add README.md
    git commit -m "chore: merged into rawgroundbeef/x402.jobs apps/api/ — see https://github.com/rawgroundbeef/x402.jobs (Phase 31)"
    git push
    ```

    STEP B: Archive the api repo (Pitfall 5 step 8):

    1. Open https://github.com/rawgroundbeef/x402-jobs-api/settings.
    2. Scroll to "Danger Zone" → "Archive this repository".
    3. Click → confirm by typing repo name.
    4. Verify the archive succeeded: repo banner now says "This repository has been archived by the owner".
    5. CRITICAL: verify the repo is STILL PRIVATE (not flipped public by the archive operation). Per RESEARCH Pattern 6 line 512: "Archiving does not flip private → public. The repo stays private."

    STEP C: Final visibility-flip pre-flight on the merged repo:

    Open https://github.com/rawgroundbeef/x402.jobs and verify:
    - LICENSE shows in the repo header (GitHub auto-detected BSL 1.1).
    - SECURITY.md is accessible at https://github.com/rawgroundbeef/x402.jobs/security/policy (GitHub's Security tab pulls SECURITY.md).
    - Default branch is `main` with the Phase 31 merge commit at HEAD.
    - CI workflow has run green on at least one PR + on the post-merge push.
    - No GitHub Issues / Discussions are pre-populated with sensitive content that shouldn't be public (defensive check; this is a fresh open-source flip).

    STEP D: Flip `rawgroundbeef/x402.jobs` to PUBLIC visibility (Pitfall 5 step 9 — THE LAST STEP):

    Option 1 (gh CLI):
    ```bash
    gh repo edit rawgroundbeef/x402.jobs --visibility public
    # If gh prompts about consequences, accept.
    ```

    Option 2 (web UI):
    1. Open https://github.com/rawgroundbeef/x402.jobs/settings.
    2. Scroll to Danger Zone → "Change repository visibility" → "Change to public".
    3. Type repo name to confirm; click "I understand, change repository visibility".

    STEP E: Verify the public flip succeeded:

    1. Open https://github.com/rawgroundbeef/x402.jobs in an INCOGNITO browser window (not signed in).
    2. Confirm:
       - Repo loads (anonymous read).
       - LICENSE displays as BSL 1.1.
       - README renders with the BSL link + quickstart.
       - SECURITY tab shows the disclosure policy.
       - Code is browsable.

    3. Public-API smoke (optional):
       ```bash
       curl -s https://api.github.com/repos/rawgroundbeef/x402.jobs | jq '.private, .visibility'
       # Expected: false, public
       ```

    4. If everything looks good → reply `approved`. Phase 31 ceremony complete.

    Failure / rollback:
    - If the public flip exposes something unexpected (a stale env var name, a wrong domain in README, etc.), flip visibility back to private IMMEDIATELY: `gh repo edit rawgroundbeef/x402.jobs --visibility private`. Fix the issue. Re-do the public flip.
    - If the archive of api repo fails: GitHub may surface a permissions issue. Re-do via web UI; the operation is idempotent.
    - DO NOT delete the api repo — Decision 3 explicitly says "Archive at end of Phase 31; do NOT delete."
  </how-to-verify>
  <resume-signal>
    Type `approved` once: (a) api repo is archived (private), (b) x402.jobs repo flipped public, (c) anonymous access verified, (d) public-API smoke confirms `visibility: public`. OR describe what failed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 7: Write 31-CONVERGENCE.md — full SC1..SC8 evidence map</name>
  <files>.planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md</files>
  <action>
    Working dir: `/Users/rawgroundbeef/Projects/x402jobs`. Run AFTER Task 6 approval.

    Use the Write tool to create `.planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md`. Suggested structure (fill in actual values from /tmp/31-05-* artifacts + the per-plan SUMMARYs + the Railway/Vercel deploy URLs captured in Tasks 4 + 5):

    ```markdown
    # Phase 31 — Convergence Evidence

    **Completed:** <YYYY-MM-DD>
    **License:** BSL 1.1 with Memeputer LLC Licensor + Sentry-style Additional Use Grant
    **Public flip:** rawgroundbeef/x402.jobs is now PUBLIC
    **Archive:** rawgroundbeef/x402-jobs-api is now archived (private, read-only)
    **Source sha imported:** <SOURCE_SHA from 31-02 SUMMARY>
    **Phase 31 main merge sha:** <merge sha from Task 3>
    **Pnpm pin (all 6 sites):** pnpm@10.6.5 (unchanged from Phase 30 — preserved invariant)

    ## Success-Criteria Map

    | SC# | ROADMAP Criterion | Status | Evidence | Source artifact |
    |-----|-------------------|--------|----------|-----------------|
    | SC1 | Single repo, single license, single CI | ✅ PASS | LICENSE present; apps/api/ exists; .github/workflows/ci.yml runs path-filtered jobs | LICENSE + apps/api/ + .github/workflows/ci.yml + 31-01/02/03 SUMMARYs |
    | SC2 | Both apps deploy cleanly to Vercel + Railway post-merge | ✅ PASS | Vercel main deploy green (Task 4); Railway re-point + boot guard clean (Task 5) | Vercel deploy URL: <url>; Railway deploy URL: <url> |
    | SC3 | `pnpm install && pnpm dev` produces working local env | ✅ PASS | Plan 02 Task 10 human checkpoint + Plan 04 Task 3 fallback smokes | 31-02 + 31-04 SUMMARYs |
    | SC4 | LICENSE, README, SECURITY.md, CONTRIBUTING.md all present | ✅ PASS | All 5 docs present (LICENSE + README + SECURITY + CONTRIBUTING + CLAUDE) | 31-01 SUMMARY + Task 1 grep gates |
    | SC5 | LICENSE names **Memeputer LLC** verbatim with locked grant text | ✅ PASS | Task 1 grep gates: `Licensor: Memeputer LLC` (1 hit), `Memeputer LLC, or its successor` (1 hit), `Apache License, Version 2.0` (1 hit) | LICENSE file + Task 1 audit artifacts |
    | SC6 | SECURITY.md release-age policy externalized + private disclosure + empty Known Unfixed | ✅ PASS | Task 1 grep gates: `minimum-release-age=4320` present, `None at public launch` present, NO HIGH-NN marked OPEN | SECURITY.md + Task 1 audit artifacts |
    | SC7 | SECURITY.md documents release-age policy externally | ✅ PASS | Same as SC6 | Same as SC6 |
    | SC8 | Vercel + Railway main deploys match 30-CONVERGENCE.md "Expected build-log assertions" | ✅ PASS | Web: pnpm@10.6.5, 0 ENOWORKSPACES, 48/48 pages. Api: pnpm@10.6.5, 0 Ignored scripts, tsup dist/index.js produced, no encryption-secret-not-set boot warnings | Task 4 + Task 5 deploy logs |

    ## Cross-Environment Pin Convergence (post-merge)

    | Site | Reference | Pin |
    |------|-----------|-----|
    | Root | package.json#packageManager | pnpm@10.6.5 |
    | Web | apps/web/vercel.json#installCommand | pnpm@10.6.5 |
    | Api | apps/api/Dockerfile RUN line | pnpm@10.6.5 |
    | Api | apps/api/package.json#packageManager | pnpm@10.6.5 |
    | CI | .github/workflows/ci.yml pnpm/action-setup version | 10.6.5 |
    | Deleted | apps/api/vercel.json (was vestigial per D-04) | (deleted) |

    Total: 5 active pin sites + 1 documented deletion. All 5 match exactly.

    ## Configuration State (Post-Phase 31)

    ### Public-facing docs
    - LICENSE: BSL 1.1, Memeputer LLC, Change Date <YYYY-MM-DD> (4 years from LICENSE-landing commit)
    - README.md: BSL link, quickstart with all 3 ports, structure block lists apps/api/
    - SECURITY.md: GitHub PVR + .npmrc policy + encryption-secret invariants + empty Known Unfixed
    - CONTRIBUTING.md: BSL link, dev orchestration (`pnpm dev` + fallbacks), conventional-commits
    - CLAUDE.md: pnpm 10.6.5 lock, .npmrc lock, BSL lock, isolated-vm invariant

    ### Workspace tooling
    - pnpm-workspace.yaml: packages [apps/*, packages/*] + ignoredBuiltDependencies: [isolated-vm] + pnpm 11 migration comment
    - package.json: 8-entry alphabetical onlyBuiltDependencies union (added bigint-buffer + isolated-vm); 9 scripts (5 original + 4 new fallback/test)
    - turbo.json: 6 tasks (added `test`); `dev: persistent: true` unchanged
    - .gitignore: stale apps/api-audit-tmp/ block removed
    - .npmrc: UNCHANGED (Phase 30 release-age policy intact)

    ### CI
    - .github/workflows/ci.yml: 3 jobs (changes/web/api), dorny/paths-filter@v3, pnpm 10.6.5 + Node 22, `shared:` filter mitigates Pitfall 2

    ### Deploys
    - Vercel: rawgroundbeef/x402.jobs → apps/web (unchanged repo target; Plan 02 didn't move it)
    - Railway: rawgroundbeef/x402.jobs → apps/api/ (RE-POINTED in Task 5 from x402-jobs-api/root)

    ### Visibility
    - rawgroundbeef/x402.jobs: PUBLIC
    - rawgroundbeef/x402-jobs-api: archived (private, read-only)

    ## Phase 28 Carry-Forward Verification

    Per 31-CONTEXT.md D-01, all 12 Phase 28 HIGHs shipped via api repo PR #32 commit c751857 on 2026-05-14. The squash-import carried them forward unchanged:

    | Artifact | Location post-merge | Carried? |
    |----------|--------------------|----------|
    | HIGH-02 migration (x402_oauth_pending + ciphertext columns) | apps/api/migrations/009_add_oauth_pending_and_encrypted_tokens.sql | ✅ verified |
    | HIGH-02 route (state nonce + dual-write encryption) | apps/api/src/routes/integrations.ts | ✅ verified (encryptSecret string present) |
    | HIGH-02 backfill script | apps/api/scripts/migrate-encrypt-x-tokens.ts | ✅ verified |
    | All other 11 HIGHs (Batches A-I) | apps/api/src/* (per HIGHS-TRIAGE.md) | ✅ verified via api test suite pass + 30-CONVERGENCE references |

    SECURITY.md `Known Unfixed Findings` section is therefore correctly empty at launch.

    ## History Audit

    - No secrets in any commit: `git log -p --all -- apps/api/` filtered for BEGIN PRIVATE KEY / wallet-backup / service_role_key patterns → empty (T-31-01 mitigation verified)
    - apps/api-audit-tmp/ never committed: `git log --all -- 'apps/api-audit-tmp/*'` → empty (T-31-06 + RESEARCH Security Domain row mitigation verified)
    - Single squash-import commit references SOURCE sha: visible in `feat(monorepo): import x402jobs-api at <SOURCE_SHA>` commit subject

    ## Deferred Items (Phase 32+)

    - Migration folder consolidation (`apps/api/migrations/` flat vs `apps/api/supabase/migrations/` timestamped) — RESEARCH Pitfall 6; defer to v3.1 audit
    - Web app test framework (apps/web has no tests; CI runs only lint + typecheck + build for web)
    - Optional action-pin tightening: replace `dorny/paths-filter@v3` with commit-SHA pin if supply-chain paranoia tightens
    - Optional `@solana/wallet-adapter-trezor` removal: would eliminate 4/5 BLOCKER-30-01-A ignored scripts
    - Optional `eslint-config-next` Rust-resolver opt-out: would eliminate the 5th BLOCKER-30-01-A ignored script

    ## Related Plan SUMMARYs

    - [31-01-SUMMARY.md](./31-01-SUMMARY.md) — BSL LICENSE + public docs
    - [31-02-SUMMARY.md](./31-02-SUMMARY.md) — squash-import + workspace reconciliation
    - [31-03-SUMMARY.md](./31-03-SUMMARY.md) — unified GitHub Actions CI workflow
    - [31-04-SUMMARY.md](./31-04-SUMMARY.md) — dev:web/dev:api/dev:inngest fallback scripts
    - [31-05-SUMMARY.md](./31-05-SUMMARY.md) — convergence + this document
    - [31-ROLLBACK.md](./31-ROLLBACK.md) — single-page rollback runbook with commit shas + Railway revert + visibility-flip-back

    ## Known Limitations

    - Public-flip is reversible (`gh repo edit ... --visibility private`) but is a strong signal to early adopters; treat as irreversible in practice.
    - Railway re-point from archived `x402-jobs-api` back to original is possible but requires re-grant of GitHub app access if the integration was revoked.
    - Memeputer LLC entity status MUST stay valid; if dissolved, the "Memeputer LLC, or its successor" clause in LICENSE handles continuity per CONTEXT D-02.
    ```

    Fill in placeholders with actual values from prior plan SUMMARYs + Task 4 + Task 5 outputs. The doc should be ~150-200 lines.
  </action>
  <verify>
    <automated>cd /Users/rawgroundbeef/Projects/x402jobs && test -f .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md && grep -q "Memeputer LLC" .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md && grep -q "SC8" .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md && grep -q "PUBLIC" .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md && grep -q "archived" .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md && grep -q "pnpm@10.6.5" .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md</automated>
  </verify>
  <done>
    - 31-CONVERGENCE.md exists with all SC1..SC8 mapped to evidence
    - Cross-environment pin convergence table populated (5 active sites + 1 deletion)
    - Phase 28 carry-forward verification table populated
    - History audit results captured
    - Deferred items listed for Phase 32+
    - All placeholders filled with actual values
  </done>
</task>

<task type="auto">
  <name>Task 8: Write 31-ROLLBACK.md — single-page rollback runbook</name>
  <files>.planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md</files>
  <action>
    Working dir: `/Users/rawgroundbeef/Projects/x402jobs`.

    Use the Write tool to create `.planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md`. Use 30-ROLLBACK.md as the structural model (similar shape). Fill in actual commit shas from /tmp/31-05-branch-shas.txt and the per-plan SUMMARYs.

    Suggested structure:

    ```markdown
    # Phase 31 — Rollback Runbook

    **Use this if:** any Phase 31 change regresses production after merge.
    **Time to fully revert:** ~15-30 minutes (depends on whether visibility-flip-back + Railway-re-point-back are needed).
    **Independence:** the merged tree state can be reverted via git; the visibility flip + archive + Railway re-point are separate concerns and may or may not need reversal depending on the failure mode.

    ## Commit Reference

    All commits on `main` branch of `rawgroundbeef/x402.jobs`. PR <PR_NUMBER> merged via squash-and-merge.

    ### Pre-merge plan branch commits (preserved in the closed branch + PR history)
    | Plan | Commit sha | What it changed |
    |------|-----------|-----------------|
    | 31-01 | `<sha-from-31-01-SUMMARY>` | LICENSE + SECURITY.md + CONTRIBUTING.md + CLAUDE.md + README.md |
    | 31-02 | `<sha-from-31-02-SUMMARY>` | apps/api/ squash-import + root tooling + cleanup deletions + lockfile regen |
    | 31-03 | `<sha-from-31-03-SUMMARY>` | .github/workflows/ci.yml — unified CI workflow |
    | 31-04 | `<sha-from-31-04-SUMMARY>` | package.json — dev:web/dev:api/dev:inngest/test scripts |

    ### Post-merge single squash commit on main
    | Plan | Merge commit | What it changed |
    |------|--------------|-----------------|
    | 31 PR squash | `<merge-sha-from-Task-3>` | All 4 plan commits combined (squash-and-merge) |

    Source repo at import time: `rawgroundbeef/x402-jobs-api` @ `<SOURCE_SHA from 31-02 SUMMARY>` (now archived, still private).

    ## Revert Sequences

    ### Symptom: Vercel main deploy fails post-merge
    Same revert path as 30-ROLLBACK.md Symptom A — revert the squash-merge commit:
    ```bash
    cd /Users/rawgroundbeef/Projects/x402jobs
    git checkout main && git pull
    git revert <merge-sha>
    git push origin main
    ```
    Vercel redeploys (~3 min). Restores pre-Phase-31 state on the web side.

    Note: this revert does NOT change Railway state. If Railway was re-pointed in Task 5, you'll need Symptom B to reverse that.

    ### Symptom: Railway main deploy fails after re-point (boot guard fires; encryption secrets missing)
    1. Open Railway dashboard → x402-jobs-api service → Settings → Source.
    2. Change Source Repository BACK to `rawgroundbeef/x402-jobs-api` (still archived; Railway can deploy from archived repos via existing integration).
    3. Reset Root Directory to default (or whatever it was pre-re-point).
    4. Save. Railway redeploys from the archived repo's last commit.
    5. Verify boot guard: api logs do NOT contain `WALLET_ENCRYPTION_SECRET environment variable is not set`.
    6. If the encryption secrets were lost during the re-point (Pitfall 4 worst case), restore from 1Password backup BEFORE the revert deploy.

    ### Symptom: Wrong content in LICENSE (legal review surfaces an issue)
    1. Fix LICENSE on a new branch:
    ```bash
    git checkout -b fix/license-correction
    # Edit LICENSE — fix the wording or licensor field
    git add LICENSE
    git commit -m "docs(31): correct LICENSE per legal review"
    git push -u origin fix/license-correction
    gh pr create --title "Fix LICENSE per legal review"
    ```
    2. Merge the fix PR.
    3. CRITICAL: do NOT amend the original Phase 31 squash-merge commit on main — that breaks git history. Always forward-fix with a new commit.

    ### Symptom: Public visibility flip exposed something unexpected (PII, broken docs, embarrassing content)
    Immediate response (urgency: minutes):
    ```bash
    gh repo edit rawgroundbeef/x402.jobs --visibility private
    # OR via web UI: Settings → Danger Zone → Change repository visibility → Make private
    ```
    Effects:
    - Anonymous access stops within seconds.
    - Stars, watches, forks from the brief public window remain (cannot be revoked).
    - GitHub search index may take 24h to drop the entry.

    Then fix the underlying issue and re-flip public when ready. The "moment of truth" public-flip is the same gh command in reverse direction.

    ### Symptom: Archive ceremony went wrong (wrong repo archived, or visibility accidentally flipped during archive)
    Archive itself is reversible:
    ```bash
    # Via web UI: rawgroundbeef/x402-jobs-api → Settings → Danger Zone → "Unarchive this repository"
    # gh CLI doesn't expose unarchive; use the web UI.
    ```
    Once un-archived, the repo accepts pushes again. If a stale push from the api repo's old maintainer flow is needed, it can land.

    ### Symptom: Local pnpm install broken after pulling Phase 31 to a developer's machine
    Common cause: developer's local pnpm CLI is on an older version (pnpm 9.x) and the merged tree references pnpm@10.6.5 across the board.

    ```bash
    # Downgrade or align local pnpm CLI:
    npm install -g pnpm@10.6.5
    # Then in repo:
    cd /Users/rawgroundbeef/Projects/x402jobs
    rm -rf node_modules apps/*/node_modules packages/*/node_modules
    pnpm install
    ```

    If install fails with `ERR_PNPM_NO_MATCHING_VERSION` from the release-age gate (.npmrc), forward-fix per 30-ROLLBACK.md Symptom D: add `minimum-release-age-exclude=<pkg-name>` to `.npmrc`, commit.

    ## Forward-Fix Alternatives (preferred to revert)

    | Failure mode | Forward fix | Effort |
    |---|---|---|
    | LICENSE wording issue | Fix on a new branch + PR; do NOT amend the squash-merge commit | 5-30 min depending on legal review |
    | SECURITY.md needs a known-issue added post-launch | Edit SECURITY.md on a new branch + PR | 10 min |
    | Web app fails after deploy | Vercel dashboard → Deployments → previous deployment → Promote | 2 min |
    | Api fails after Railway re-point with `ERR_PNPM_NO_MATCHING_VERSION` | Add `minimum-release-age-exclude=<pkg-name>` to .npmrc, commit, push (auto-deploys) | 5 min |
    | New Ignored build scripts: post-merge | Add to `pnpm.onlyBuiltDependencies`, commit (per 30-ROLLBACK.md Symptom E) | 5 min |
    | dorny/paths-filter@v3 vulnerability disclosed → need to pin to SHA | Edit .github/workflows/ci.yml, replace `@v3` with `@<commit-sha>`, commit | 5 min |

    ## Cross-Phase Dependencies After Revert

    - **Phase 28 HIGH remediation** is independent — no impact from a Phase 31 revert (the Highs were shipped via api PR #32 BEFORE Phase 31 ran).
    - **Phase 30 pnpm@10.6.5 baseline** is independent — Phase 31 inherits but does not modify the baseline.
    - **Phase 32 (Agent SDK)** assumes Phase 31 landed (merged repo, BSL license, single CI). If you revert past 31-02 (the squash-import), Phase 32 cannot proceed; flag in `.planning/STATE.md`.

    ## Recovery Verification After Revert

    Confirm recovery is clean by re-running the cross-environment audit:
    ```bash
    cd /Users/rawgroundbeef/Projects/x402jobs
    pnpm --version
    pnpm install
    pnpm --filter @x402jobs/web build
    # If apps/api still exists (partial revert), also:
    pnpm --filter x402-jobs-api build && pnpm --filter x402-jobs-api test
    ```

    Also re-verify Vercel + Railway dashboards show the expected last-known-good state.
    ```

    Fill in `<sha-from-...>` placeholders with actual values from `/tmp/31-05-branch-shas.txt` + each plan's SUMMARY. The doc should be ~80-120 lines.
  </action>
  <verify>
    <automated>cd /Users/rawgroundbeef/Projects/x402jobs && test -f .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && grep -q "31-01" .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && grep -q "31-02" .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && grep -q "31-03" .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && grep -q "31-04" .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && grep -q "Railway" .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && grep -q "visibility" .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && ! grep -q "<sha-from-" .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md</automated>
  </verify>
  <done>
    - 31-ROLLBACK.md exists with actual commit shas filled in (no `<sha-from-...>` placeholders remaining)
    - Railway re-point reversal procedure documented
    - Visibility-flip-back procedure documented
    - Forward-fix alternatives section present (preferred over revert for most modes)
    - Cross-phase dependencies after revert documented
  </done>
</task>

<task type="auto">
  <name>Task 9: Update STATE.md + ROADMAP.md to mark Phase 31 complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Working dir: `/Users/rawgroundbeef/Projects/x402jobs`. Run after Task 7 + Task 8 docs land.

    Step 1: Update STATE.md frontmatter + body. Use the Write tool to rewrite or use the existing structure — read STATE.md first to preserve unrelated fields. Target changes:

    Frontmatter `progress` block — update counts:
    ```yaml
    progress:
      total_phases: 10
      completed_phases: 9
      total_plans: 21
      completed_plans: 20
      percent: 95
    ```
    (If counts are off, recalculate from actual ROADMAP.md state — 15 prior plans + 5 from Phase 31 = 20 completed, 21 total if Phase 32 has a 1-plan placeholder.)

    Frontmatter `stopped_at` + `last_updated` + `last_activity` fields — update to current date.

    Body `Current Position` section:
    ```markdown
    **Phase:** 32
    **Plan:** Not started
    **Status:** Phase 31 complete; rawgroundbeef/x402.jobs is now PUBLIC under BSL 1.1
    ```

    Body `v3.0 Progress` bar:
    ```
    v3.0 Progress: [█████░░░░░] 3/7 phases (Phase 27 ✓, Phase 28 Criticals + Highs ✓, Phase 29 ✓, Phase 30 ✓, Phase 31 ✓)
    ```

    Body `Session Continuity` section:
    ```markdown
    **Last session:** <YYYY-MM-DD>
    **Stopped at:** Phase 31 (Monorepo Merge + BSL 1.1) shipped; rawgroundbeef/x402.jobs is now PUBLIC under BSL 1.1; rawgroundbeef/x402-jobs-api archived (private, read-only); Railway re-pointed to apps/api/; Vercel unchanged; cross-environment pnpm@10.6.5 pin across 5 sites
    **Resume with:** Plan Phase 32 (Agent SDK design) — `/gsd-plan-phase 32`.

    **Active context for next session:**
    - Public repo URL: https://github.com/rawgroundbeef/x402.jobs
    - Archived api repo: https://github.com/rawgroundbeef/x402-jobs-api (private, read-only)
    - License: BSL 1.1 with Memeputer LLC as Licensor (verbatim per 31-CONTEXT.md D-02)
    - Change Date: <YYYY-MM-DD from 31-01 SUMMARY> (4 years from LICENSE-landing)
    - CI workflow live at .github/workflows/ci.yml (dorny/paths-filter@v3, pnpm 10.6.5)
    - Both encryption secrets verified surviving Railway re-point (WALLET_ENCRYPTION_SECRET, INTEGRATION_ENCRYPTION_SECRET)
    ```

    Body trailer — append:
    ```
    _v3.0 Phase 31 (Monorepo Merge + BSL 1.1) complete: <YYYY-MM-DD>_
    ```

    Step 2: Update ROADMAP.md Phase 31 section. Use the Write tool (read ROADMAP.md first to preserve other sections). Target changes:

    Phase 31 Plans block — change all 5 entries from `[ ]` to `[x]`:
    ```
    Plans:
    - [x] 31-01-PLAN.md -- BSL 1.1 LICENSE + README + SECURITY.md + CONTRIBUTING.md + CLAUDE.md (drafted while still private)
    - [x] 31-02-PLAN.md -- Squash-import x402-jobs-api → apps/api/ + workspace reconciliation + cleanup deletions
    - [x] 31-03-PLAN.md -- Unified GitHub Actions CI workflow with dorny/paths-filter@v3
    - [x] 31-04-PLAN.md -- Local-dev orchestration polish (dev:web / dev:api / dev:inngest fallback scripts + test alias)
    - [x] 31-05-PLAN.md -- Public-flip ceremony + convergence (Railway re-point + archive + visibility flip + CONVERGENCE.md + ROLLBACK.md + STATE.md)
    ```

    Add or update the Progress table to include Phase 31:
    ```
    | 31 - Monorepo Merge + BSL 1.1 | Complete | 5/5   | SC1..SC8 |
    ```

    If the ROADMAP doesn't have a v3.0 progress table yet, add one or append the row to the existing v2.0 table footer.

    Step 3: Commit both updates in a single commit:
    ```bash
    git add .planning/STATE.md .planning/ROADMAP.md

    cat > /tmp/31-05-state-commit-msg.txt <<'MSG'
    docs(state): mark Phase 31 (Monorepo Merge + BSL 1.1) complete

    Phase 31 shipped: rawgroundbeef/x402.jobs is now PUBLIC under BSL 1.1 with
    Memeputer LLC as Licensor. The x402jobs-api repo is archived (private,
    read-only). Railway re-pointed to apps/api/; Vercel unchanged. Cross-
    environment pnpm@10.6.5 pin across 5 sites (root + Vercel + Railway
    Dockerfile + api package.json + .github/workflows/ci.yml).

    Phase 28 HIGHs already shipped via api repo PR #32 commit c751857
    (2026-05-14) and carried forward via the squash-import. SECURITY.md
    "Known Unfixed Findings" section is empty at launch per 31-CONTEXT.md D-01.

    Phase 30 SC6 ("CI green on both repos") retroactively satisfied by
    plan 31-03's unified workflow.

    Plans landed:
      31-01 - BSL 1.1 LICENSE + public-facing docs
      31-02 - Squash-import + workspace reconciliation
      31-03 - Unified GitHub Actions CI workflow
      31-04 - dev:web/dev:api/dev:inngest fallback scripts
      31-05 - Public-flip ceremony + convergence (this commit)

    Refs: Phase 31 SC1, SC2, SC3, SC4, SC5, SC6, SC7, SC8
    Refs: 30-CONVERGENCE.md SC6 retroactive
    Refs: 31-CONVERGENCE.md (evidence map)
    Refs: 31-ROLLBACK.md (single-page revert runbook)
    MSG

    git commit -F /tmp/31-05-state-commit-msg.txt
    git push origin main
    ```
  </action>
  <verify>
    <automated>cd /Users/rawgroundbeef/Projects/x402jobs && grep -q "Phase 31.*complete" .planning/STATE.md && grep -q "Phase 32" .planning/STATE.md && grep -qE "^- \[x\] 31-05-PLAN" .planning/ROADMAP.md && grep -qE "Phase 31.*Complete.*5/5" .planning/ROADMAP.md && git log -1 --format="%s" | grep -q "Phase 31"</automated>
  </verify>
  <done>
    - STATE.md frontmatter shows Phase 31 in completed counts (9/10 phases, 20/21 plans)
    - STATE.md Current Position points at Phase 32
    - STATE.md Session Continuity captures Phase 31 closure + active context
    - ROADMAP.md Phase 31 plans all `[x]` checked
    - ROADMAP.md Progress table updated with Phase 31 row
    - Single commit on main with `docs(state): mark Phase 31...` subject
  </done>
</task>

<task type="auto">
  <name>Task 10: Commit 31-CONVERGENCE.md + 31-ROLLBACK.md to main</name>
  <files>(git commit only; the docs were written in Task 7 + Task 8)</files>
  <action>
    Working dir: `/Users/rawgroundbeef/Projects/x402jobs`.

    Stage and commit the two docs:
    ```bash
    git add .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md

    cat > /tmp/31-05-docs-commit-msg.txt <<'MSG'
    docs(31): add 31-CONVERGENCE.md + 31-ROLLBACK.md for Phase 31 closure

    Cross-environment convergence evidence + single-page rollback runbook
    matching the Phase 30 convention (30-CONVERGENCE.md + 30-ROLLBACK.md).

    31-CONVERGENCE.md:
      - Maps SC1..SC8 to evidence (deploy URLs, log snippets, grep gate results)
      - Cross-environment pnpm@10.6.5 pin convergence table (5 sites)
      - Phase 28 carry-forward verification (Batch H migration + route + script)
      - History audit (no secrets ever committed; apps/api-audit-tmp never tracked)
      - Deferred items for Phase 32+

    31-ROLLBACK.md:
      - 4 plan branch commit shas + 1 main merge commit sha
      - Revert sequences per failure symptom (Vercel / Railway / LICENSE / visibility)
      - Forward-fix alternatives (preferred over revert)
      - Cross-phase dependencies after revert

    Refs: Phase 31 closure
    MSG

    git commit -F /tmp/31-05-docs-commit-msg.txt
    git push origin main
    ```
  </action>
  <verify>
    <automated>cd /Users/rawgroundbeef/Projects/x402jobs && test -f .planning/phases/31-monorepo-merge-bsl/31-CONVERGENCE.md && test -f .planning/phases/31-monorepo-merge-bsl/31-ROLLBACK.md && git log -2 --format="%s" | grep -qE "31-CONVERGENCE|Phase 31"</automated>
  </verify>
  <done>
    - 31-CONVERGENCE.md + 31-ROLLBACK.md committed to main
    - Phase 31 closure docs are now part of the public repo (per the public-flip in Task 6)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-merge cross-grep audit → merge decision | Audit gates are the only barrier between locked-decision drift and main-branch landing |
| Vercel deploy log → production confidence | Build log is the only auditable signal that pnpm pin + ENOWORKSPACES fix held |
| Railway re-point UI → encryption-secret survival | Pitfall 4's catastrophic-loss scenario depends on this UI step being done correctly |
| Public visibility flip → world-readable repo history | All prior commits on `main` become publicly accessible; secrets in history = secrets in the public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Tampering | LICENSE Licensor field could be edited post-flip to `Ben Tatum` or `x402.jobs` | mitigate | Task 1 grep gate enforces `Licensor: Memeputer LLC` exactly. CLAUDE.md from Plan 01 locks BSL invariant for future AI sessions. |
| T-31-05 | Tampering | Railway encryption-secret mis-paste during re-point causes runtime decryption failures (catastrophic for OAuth tokens + wallets) | mitigate | Task 5 explicitly: (a) pre-flight visual verification of both secrets BEFORE source change, (b) post-build runtime log inspection for boot-guard fire, (c) cross-service smoke confirming round-trip decryption. RESEARCH Pitfall 4 + Assumption A3 directly addressed. |
| T-31-08 | Tampering | Visibility flip happens BEFORE history audit confirms clean | mitigate | Task 6 strict pre-flight checklist enforces ceremony order: docs → import → CI → smoke → merge → archive THEN public flip LAST. Task 1's history audit (no secrets, no audit-tmp) is the gate. |
| T-31-01 | Information Disclosure | Pre-flip git history contains secret in archived api repo's history (not in this repo, but the api repo's archive could later be compromised) | accept (Phase 27 covered the pre-Phase-31 history audit on the api repo) | The api repo's history is preserved in the archived private remote. Phase 27 audited it once. If a secret IS in api repo history, it's already there pre-archive; archiving doesn't expose anything new. The squash-import dropped that history (Decision 3); the public x402.jobs repo's history starts fresh. |
| T-31-26 | Information Disclosure | Public flip exposes a stale env var name, internal URL, or test credential left in apps/api/ source | mitigate | Plan 02 Task 8 + Task 9 pre/post-commit secret audit. Task 1 of this plan re-runs the audit at merge time. Defense in depth via 3 audits. |
| T-31-27 | Repudiation | No auditable record of "we verified SC1..SC8" → future maintainer can't tell whether the public-flip happened with all gates passed | mitigate | 31-CONVERGENCE.md IS the audit record. Task 7 maps each SC to concrete evidence (log snippet, deploy URL, grep result). |
| T-31-28 | Tampering | Rollback runbook has `<sha-from-...>` placeholders that aren't filled, making it useless in an emergency | mitigate | Task 8 verify gate enforces `! grep -q '<sha-from-' 31-ROLLBACK.md`. All shas filled at write time. |
| T-31-29 | Denial of Service | Railway re-point fails partway (env vars survived but source change errored) — service in inconsistent state | accept (with recovery path) | Task 5 recovery: Railway → Settings → Source → revert to `rawgroundbeef/x402-jobs-api`. The archived api repo is still deployable via existing PAT. Re-try the re-point after diagnosing. |
| T-31-30 | Repudiation | STATE.md/ROADMAP.md not updated post-merge → next session's planner sees Phase 31 as in-progress | mitigate | Task 9 updates both files in a single commit. Task 9 only runs AFTER Task 6 public-flip approval. |
</threat_model>

<verification>
At plan completion:
- Task 1 grep-gate audit pass: BSL invariants, SECURITY.md externalizations, 5-site pnpm pin, no pnpm@9 stragglers, no apps/api-audit-tmp references, no secrets in history
- Task 2 PR pre-merge review approved
- Task 3 PR merged to main via squash; merge sha captured
- Task 4 Vercel main deploy green per 30-CONVERGENCE.md "Expected build-log assertions"
- Task 5 Railway re-pointed to apps/api/; both encryption secrets survived; boot guard clean; cross-service smoke passed
- Task 6 archive ceremony complete (api repo archived, private); rawgroundbeef/x402.jobs is PUBLIC; anonymous access verified
- Task 7 31-CONVERGENCE.md exists with all 8 SCs mapped to evidence
- Task 8 31-ROLLBACK.md exists with actual commit shas (no `<sha-...>` placeholders)
- Task 9 STATE.md + ROADMAP.md updated; Phase 31 marked complete; Session Continuity points at Phase 32
- Task 10 closure docs committed to main
- Final state: 9/10 phases complete in v3.0; public open-source repo live under BSL 1.1
</verification>

<success_criteria>
- ✓ SC1: Single repo, single license, single CI (LICENSE + apps/api/ + .github/workflows/ci.yml all on main)
- ✓ SC2: Vercel + Railway both deploy cleanly post-merge (Task 4 + Task 5 verified)
- ✓ SC3: Local dev path verified end-to-end (Plan 02 Task 10 + Plan 04 Task 3)
- ✓ SC4: LICENSE, README, SECURITY.md, CONTRIBUTING.md all present (Task 1 grep gate)
- ✓ SC5: LICENSE Licensor = Memeputer LLC verbatim (Task 1 grep gate)
- ✓ SC6: SECURITY.md release-age policy + private disclosure + empty Known Unfixed (Task 1 grep gate)
- ✓ SC7: SECURITY.md externalizes release-age policy (same as SC6 evidence)
- ✓ SC8: Vercel + Railway main-branch deploys satisfy 30-CONVERGENCE.md (Task 4 + Task 5 deploy logs)
- ✓ Phase 30 SC6 retroactive: unified CI shipped in plan 31-03; deploy-preview-only validation regime ends
- ✓ Public repo live: anonymous access verified; BSL 1.1 license displayed
- ✓ Archived api repo (private, read-only): preserves provenance per Decision 3
- ✓ Convergence + rollback docs produced
- ✓ STATE.md + ROADMAP.md updated; Phase 31 closes cleanly
</success_criteria>

<rollback>
This plan creates docs + updates STATE/ROADMAP + executes UI ceremonies. It does NOT itself land production code (the production code landed in plans 01-04 and was merged in Task 3 of this plan).

**If a Task fails:**
- Task 1 audit fails: STOP, do NOT merge. Address the failing invariant in a forward-fix commit on the plan branch.
- Task 3 merge succeeds but Task 4 Vercel deploy fails: revert per 31-ROLLBACK.md Symptom A.
- Task 4 succeeds but Task 5 Railway re-point fails (encryption secrets lost): per Task 5 recovery path, revert Railway source change.
- Task 5 succeeds but Task 6 public-flip exposes something bad: flip back to private immediately (`gh repo edit rawgroundbeef/x402.jobs --visibility private`), fix, re-flip.

**If Task 9's STATE.md/ROADMAP.md update is the only thing missing:**
Simply commit those updates separately. No revert needed — STATE.md is internal-planning state, not deployed code.

**If 31-CONVERGENCE.md / 31-ROLLBACK.md have wrong content:**
Forward-fix with a new docs commit. They live in `.planning/` so are not load-bearing for deploys.

**Worst case (all of Phase 31 must be reverted):**
1. Flip x402.jobs visibility back to private.
2. Un-archive x402-jobs-api repo.
3. Railway: revert Source Repository back to x402-jobs-api.
4. Git: `git revert <Phase-31-merge-sha>` on main of x402.jobs; push.
5. Update STATE.md to mark Phase 31 as incomplete; plan 31 closure docs (CONVERGENCE.md, ROLLBACK.md) stay in place as historical context.

Time-to-full-revert: ~30-45 minutes (depends on Railway re-deploy speed + visibility-flip propagation).
</rollback>

<output>
After completion, create `.planning/phases/31-monorepo-merge-bsl/31-05-SUMMARY.md` capturing:
- Phase 31 main merge sha (captured in Task 3)
- Vercel deploy URL (Task 4) + key build-log assertions verified (pnpm@10.6.5, 0 ENOWORKSPACES, 48 pages)
- Railway deploy URL (Task 5) + key build-log assertions verified + boot-guard clean + both encryption secrets survived
- Archive ceremony confirmation (api repo archived, still private)
- Public-flip confirmation (anonymous access verified)
- All 5 plan SUMMARYs referenced
- Reference to 31-CONVERGENCE.md + 31-ROLLBACK.md
- Total plan-count for Phase 31 (5 plans across 4 waves)
- Confirmation that STATE.md + ROADMAP.md updates landed
- Pointer to Phase 32 (Agent SDK) as the next phase
- Total elapsed time for Phase 31 ceremony (audit + merge + Vercel + Railway + archive + flip)
- Any deviations from locked decisions (expected: NONE)
</output>
